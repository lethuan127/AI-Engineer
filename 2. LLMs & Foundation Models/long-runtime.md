1. Streaming
1. Run in background mode (OpenAI Support: The latency gap for background responses is still a known tradeoff, and we don’t have a completion date to share right now)

2. Use smaller format like markdown instead html
3. Multiple turn output like file edit
4. Fallback solution