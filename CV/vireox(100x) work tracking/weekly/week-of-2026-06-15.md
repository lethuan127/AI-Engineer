# Week of 15 June 2026

> **Range:** Mon 15 Jun – Sun 21 Jun 2026  
> **Activity:** 68 commits across 3 repos · 21 Linear tickets

| Repository | Commits | Domain |
|---|---|---|
| `100x-agent-runtime` | 46 | Deep Agents runtime |
| `vireox-runtime-plugin` | 19 | Vireox runtime plugin |
| `100x-agent-hub` | 3 | Backend agent platform |

---

## 🎫 Linear tickets

**Completed (10)**

- ✅ **AIP-165** — Long-term agent memory (per-user, cross-thread) _(Medium)_
- ✅ **AIP-163** — Local-shell files written under the configured workspace root _(Medium)_
- ✅ **AIP-126** — Rollout Deep Agents as a replacement for agno _(—)_
- ✅ **AIP-158** — Pilot vireox-all-agents-flat (single-agent Deep Agents team) + evaluation harness _(Medium)_
- ✅ **AIP-164** — Curate runtime base system prompt _(Medium)_
- ✅ **AIP-159** — Resilient tool errors, full-plugin sandbox seeding, sandbox env vars _(Medium)_
- ✅ **AIP-151** — Fix skills never loading in agent runtime _(Medium)_
- ✅ **AIP-154** — Run the Retails Eval (from Charlie) _(High)_
- ✅ **AIP-132** — Move workflow-definition generation to Temporal _(Medium)_
- ✅ **CENG-522** — Break Responses chain on day change so datetime stays live _(Urgent, `Bug`)_

**In progress / opened (3)**

- 🔵 **AIP-152** — Scheduled Tools on new Runtime _(High)_
- 📋 **AIP-122** — Allow agent self-managed skills _(Backlog, Medium)_
- 📋 **AIP-111** — Improve UX for viewing report error details _(Backlog, Low)_

**Released — v2.16.0 (8)**

- ✅ **AIP-78** — Migrate workflow session storage from MongoDB to PostgreSQL _(High)_
- ✅ **AIP-36** — Fix "document too large" error saving workflow sessions _(High, `Bug`)_
- ✅ **AIP-80** — Consolidate Python formatter & linter on Ruff _(Medium)_
- ✅ **AIP-39** — Fix invalid Notification Session ID link on Report Information page _(Medium, `Bug`)_
- ✅ **CENG-100** — Agent hallucinates current date instead of system time _(Medium, `Bug`)_
- ✅ **CENG-98** — [BUG] Router unable to query data _(High, `Bug`)_
- ✅ **ORG-3** — Fix HTML DOCTYPE extraction to support XHTML transitional _(High, `Bug`)_
- ✅ **PENG-238** — Implement Claude Opus 4.8 in Agent Hub _(Medium, `Feature`)_

---

## `100x-agent-runtime` — Deep Agents runtime (46)

- `2026-06-15` AIP-158: stabilize parallel tool-result order for prompt caching
  > create_agent dispatches parallel tool calls via the Send API, so a turn that
  > fires several tools lands its ToolMessages in completion order — which varies
  > turn to turn and breaks the cached prompt prefix at the reordered block.
  >
  > CacheStableMessageOrderMiddleware reorders each AIMessage's trailing ToolMessage
  > block into tool_calls id order on the outgoing model request (state untouched),
  > so the prefix is byte-stable across turns and prompt caching keeps hitting.
  > Wired into the root agent next to ToolErrorMiddleware.
- `2026-06-15` Disable authentication dependency for model listing endpoint to allow LibreChat requests to fetch models. This change is temporary and will be re-enabled later.
- `2026-06-15` AIP-159: resilient tool errors, full-plugin sandbox seeding, sandbox env vars
  > - ToolErrorMiddleware: convert a tool ToolException into an error ToolMessage
  > so an MCP/tool 500 no longer fails the whole turn (wired into the root agent).
  > - Sandbox bootstrap: seed each plugin's full tree (one zip per plugin) into the
  > Daytona sandbox, re-uploading only when its content hash drifts; extract via
  > python3 -m zipfile. Closes the gap where hooks/, harness/, .mcp.json were missing.
  > - claude_sandbox_env: set CLAUDE_PROJECT_DIR / CLAUDE_PLUGIN_ROOT on the sandbox
  > backend (local-shell env + inherit_env; Daytona env_vars).
  > - Add ClaudeHooksMiddleware bridge (not wired into the builder) and
  > scripts/trace_report.py for trace-based usage/duration reporting.
  > - Unit tests for all of the above.
- `2026-06-16` AIP-163: local-shell uses RUNTIME_FILES_WORKSPACE_ROOT as umbrella virtual root (#26)
  > With virtual_mode=True, a single LocalShellBackend root served both skill
  > reads (needs plugin_root) and the files workspace, so a configured
  > RUNTIME_FILES_WORKSPACE_ROOT was silently re-anchored under plugin_root
  > (/app/plugins/var/lib/app-files/...).
  >
  > Treat RUNTIME_FILES_WORKSPACE_ROOT as the local-shell backend's virtual root:
  > plugins discovered under RUNTIME_PLUGIN_ROOT are copied into <root>/<plugin>
  > (seed_local_workspace) so skills resolve, and session files land at
  > <root>/<session>. Daytona keeps the var as an absolute in-sandbox path,
  > distinguished by the backend's _sandbox attribute. Unset -> legacy local-shell
  > behavior (root_dir=plugin_root, no copy).
  >
  > …
- `2026-06-16` AIP-163: local-shell uses RUNTIME_FILES_WORKSPACE_ROOT as umbrella virtual root
  > With virtual_mode=True, a single LocalShellBackend root served both skill
  > reads (needs plugin_root) and the files workspace, so a configured
  > RUNTIME_FILES_WORKSPACE_ROOT was silently re-anchored under plugin_root
  > (/app/plugins/var/lib/app-files/...).
  >
  > Treat RUNTIME_FILES_WORKSPACE_ROOT as the local-shell backend's virtual root:
  > plugins discovered under RUNTIME_PLUGIN_ROOT are copied into <root>/<plugin>
  > (seed_local_workspace) so skills resolve, and session files land at
  > <root>/<session>. Daytona keeps the var as an absolute in-sandbox path,
  > distinguished by the backend's _sandbox attribute. Unset -> legacy local-shell
  > behavior (root_dir=plugin_root, no copy).
  >
  > …
- `2026-06-16` AIP-159 / AIP-158: resilient tool errors, full-plugin sandbox seeding, prompt-cache stability (#24)
  > * AIP-159: resilient tool errors, full-plugin sandbox seeding, sandbox env vars
  >
  > - ToolErrorMiddleware: convert a tool ToolException into an error ToolMessage
  > so an MCP/tool 500 no longer fails the whole turn (wired into the root agent).
  > - Sandbox bootstrap: seed each plugin's full tree (one zip per plugin) into the
  > Daytona sandbox, re-uploading only when its content hash drifts; extract via
  > python3 -m zipfile. Closes the gap where hooks/, harness/, .mcp.json were missing.
  > - claude_sandbox_env: set CLAUDE_PROJECT_DIR / CLAUDE_PLUGIN_ROOT on the sandbox
  > backend (local-shell env + inherit_env; Daytona env_vars).
  > - Add ClaudeHooksMiddleware bridge (not wired into the builder) and
  > scripts/trace_report.py for trace-based usage/duration reporting.
  > - Unit tests for all of the above.
  > …
- `2026-06-16` AIP-159: bump vulnerable transitive deps to clear pip-audit
  > Resolves 14 advisories across 4 packages flagged by the dependency scan:
  > aiohttp 3.14.0->3.14.1, cryptography 48.0.0->49.0.0,
  > python-multipart 0.0.29->0.0.32, starlette 1.1.0->1.3.1.
  > Lockfile-only; pip-audit clean, 313 unit tests pass, app boots.
- `2026-06-16` AIP-159: ruff-format scripts/trace_report.py (pinned formatter)
- `2026-06-16` AIP-159: apply ruff format
- `2026-06-16` AIP-159: make GET /v1/models intentionally public (LibreChat model fetch)
  > LibreChat fetches the model list to populate its picker before the user supplies
  > credentials, and the payload is non-sensitive (team folder names only). Drop the
  > auth dependency from the model-list routes (no commented-out code), document the
  > rationale, and assert both /v1/models and /librechat/v1/models are public. Auth
  > stays enforced on the chat-completions routes where a team actually runs.
- `2026-06-16` AIP-159: address Bugbot review — subagent middleware, symlink seeding, Daytona env on reuse
  > - Apply ToolErrorMiddleware + CacheStableMessageOrderMiddleware to subagents too
  > (new runtime_agent_middleware() shared by the builder and build_subagent); a
  > tool 500 inside a subagent no longer aborts the whole turn.
  > - Sandbox seeding: include symlinked regular files again (drop the blanket
  > is_symlink() skip; the resolve()+relative_to(root) guard still drops escapes)
  > and dedupe arcnames so a file and a symlink to it don't double-write.
  > - write_sandbox_env(): persist CLAUDE_PROJECT_DIR/CLAUDE_PLUGIN_ROOT to the
  > sandbox shell profile in the seed step, so sandboxes rediscovered by label
  > (which miss create-time env_vars) still get them.
- `2026-06-17` AIP-164: curate runtime base system prompt; defer identity/clarifying to team prompt
  > Replace the deepagents library BASE_AGENT_PROMPT for the runtime's provider
  > with a curated copy: drop the generic identity line, the alerting-specific
  > clarifying lines, and the duplicate "Clarifying Requests" block (the team
  > prompt owns identity and clarifying policy). Also remove the redundant runtime
  > identity line from _build_system_prompt.
  >
  > Installed via the official deepagents harness profile
  > (register_runtime_harness_profile, provider "openai", wired in app lifespan).
  > The profile scopes prompts by provider, so the curated base also replaces each
  > subagent's authored prompt on that provider.
- `2026-06-18` AIP-165: inject today's date (user timezone) into the user-profile note
  > _user_profile_note now appends "- Today: <weekday>, YYYY-MM-DD (<tz>)", computed in
  > the user's x-timezone (UTC fallback for a missing/unparseable zone), so the agent can
  > reason about relative dates in their local time. The date is always available, so the
  > note always renders (return type is now str). An injectable `now` keeps it testable.
- `2026-06-18` AIP-165: memory guide loads, episodic index discovery, user-profile prompt block
  > Memory (spec 008 follow-up):
  > - manage-memory SKILL.md: its description was invalid YAML (a `): ` colon-space),
  > so SkillsMiddleware silently dropped the whole skill and the agent lost its memory
  > map. Rewrite it as valid YAML carrying the type->location map (always in context):
  > semantic AGENTS.md, procedural skills/, episodic episodes/.
  > - episodic.py: the always-on "recent conversations" note now names
  > /memory/user/episodes/_index.md so the agent can discover and page the full episode
  > collection beyond the inline recent list (static pointer; cache-stable).
  >
  > User-profile prompt block:
  > - Inject the end user's identity (name, email, IANA timezone) into the root system
  > prompt. Name/email come from the OIDC userinfo profile (UserinfoProfile,
  > …
- `2026-06-18` AIP-165: long-term agent memory (per-user + per-org, filesystem-backed) (#29)
  > * AIP-165: long-term agent memory (per-user + per-org, filesystem-backed)
  >
  > Spec 008. Cross-thread memory via deepagents primitives: CompositeBackend
  > routes /memory/{user,org,guide} to host FilesystemBackends under
  > .runtime-memory; semantic (memory=), procedural+guide (skills=), and
  > root-only episodic transcripts. One config flag; off = unchanged. D-032.
  >
  >
  > * AIP-165: episodes as a collection (index + recency note), read-only via permissions, frontmatter transcripts
  >
  > - Episodic memory is now a collection: per-session frontmatter-Markdown transcripts
  > + a recency-sorted `_index.md` manifest (upserted per turn, deduped by session).
  > …
- `2026-06-18` AIP-165: fix CI (ruff) + Bugbot findings (session-id traversal, meta/disk id mismatch)
  > - Sanitize the x-session-id before using it as the episode filename: a crafted
  > id like `../../organization/...` no longer escapes the user's episodes dir
  > (Bugbot HIGH path traversal). The sanitized key flows to the transcript,
  > manifest, and recall pointers consistently.
  > - Expose sanitized on-disk keys (`user_dir`/`org_dir`) in
  > runtime_meta.long_term_memory so they match `.runtime-memory/users/<dir>/`
  > (Bugbot MEDIUM: raw id mismatched the disk path); eval_chat reads `user_dir`.
  > - Exclude the current session from the "recent conversations" note so its
  > advancing timestamp doesn't churn the cached system prompt each turn.
  > - getattr consistency in _rendered_message_count.
  > - ruff-format eval_chat.py (the CI pre-commit failure).
  >
  > …
- `2026-06-18` AIP-165: add runtime-evals skill (scenario-based self-eval over the live API)
  > A Claude Code skill to drive POST /v1/chat/completions with pre-defined
  > scenarios against a running runtime and self-judge the results (default
  > endpoint local).
  >
  > - scripts/eval_chat.py — stdlib SSE client, structured/--json output, token
  > resolution from --token / env / .env (RUNTIME_EVAL_TOKEN / MOCK_ACCESS_TOKEN),
  > --token-env for a 2nd identity, surfaces meta.long_term_memory + memory dir.
  > - scenarios/memory.json — the long-term-memory suite (semantic cross-thread,
  > per-user isolation, org-shared, procedural skill, episodic) + scenarios/README.md schema.
  > - SKILL.md — runbook (preflight, real-vs-mock, identity/auth, reporting).
- `2026-06-18` AIP-165: episodes as a collection (index + recency note), read-only via permissions, frontmatter transcripts
  > - Episodic memory is now a collection: per-session frontmatter-Markdown transcripts
  > + a recency-sorted `_index.md` manifest (upserted per turn, deduped by session).
  > A "recent conversations" note is rendered from the manifest into the system
  > prompt, so recency/keyword recall works without the agent discovering the dir.
  > - Read-only regions (built-in guide + episodes) enforced by a deepagents
  > FilesystemPermission write-deny passed to create_deep_agent(permissions=...),
  > replacing the bespoke ReadOnlyFilesystemBackend (removed).
  > - Episode transcripts are Markdown with YAML frontmatter (session_id, updated_at,
  > messages, title).
  > - Built-in manage-memory guide rewritten to skill-creator standards + a
  > references/authoring-skills.md (progressive disclosure).
  > - Docs aligned: spec FR-5, plan (ADR-5, OQ-1/OQ-4 resolved), runtime/AGENTS.md,
  > …
- `2026-06-18` AIP-165: long-term agent memory (per-user + per-org, filesystem-backed)
  > Spec 008. Cross-thread memory via deepagents primitives: CompositeBackend
  > routes /memory/{user,org,guide} to host FilesystemBackends under
  > .runtime-memory; semantic (memory=), procedural+guide (skills=), and
  > root-only episodic transcripts. One config flag; off = unchanged. D-032.
- `2026-06-18` AIP-126: sandbox skill deps, nested output links, prompt curation, lifecycle fix (#27)
  > * AIP-164: curate runtime base system prompt; defer identity/clarifying to team prompt
  >
  > Replace the deepagents library BASE_AGENT_PROMPT for the runtime's provider
  > with a curated copy: drop the generic identity line, the alerting-specific
  > clarifying lines, and the duplicate "Clarifying Requests" block (the team
  > prompt owns identity and clarifying policy). Also remove the redundant runtime
  > identity line from _build_system_prompt.
  >
  > Installed via the official deepagents harness profile
  > (register_runtime_harness_profile, provider "openai", wired in app lifespan).
  > The profile scopes prompts by provider, so the curated base also replaces each
  > subagent's authored prompt on that provider.
  > …
- `2026-06-18` AIP-126: raise langchain security floor to 1.3.9 to clear pip-audit gate
  > CI's pip-audit --strict flags langchain 1.3.2 (GHSA-gr75-jv2w-4656, fixed in
  > 1.3.9) — a transitive dep via deepagents/langchain-*. Add a langchain>=1.3.9
  > constraint floor alongside the existing aiohttp one and relock. Full unit +
  > integration suite passes on the bumped resolution (langchain 1.3.9,
  > langchain-core 1.4.7, langgraph 1.2.5).
- `2026-06-18` AIP-126: configurable daytona snapshot (DAYTONA_SNAPSHOT) for sandbox creation
  > The provider created sandboxes from the Daytona org-default snapshot, so rolling
  > the sandbox image forward meant changing the Daytona stack env. Add a
  > DAYTONA_SNAPSHOT setting that the provider passes to CreateSandboxFromSnapshotParams,
  > so the runtime owns which snapshot it boots from. Unset defers to the org default
  > (unchanged behavior).
- `2026-06-18` AIP-126: collect nested workspace outputs so download links work
  > Output detection and input re-seed listed the workspace non-recursively (als)
  > and flattened paths to basenames, so a file produced in a subfolder was never
  > registered and got no download link. Walk the workspace recursively via als,
  > keep the workspace-relative path as the record filename (so the link shows
  > charts/report.pdf), skip scratch trees (node_modules/, .git/, __pycache__/,
  > dotfiles, temp suffixes), and re-seed nested outputs in place (creating parent
  > dirs) without treating them as stale.
- `2026-06-18` AIP-126: drop redundant daytona sandbox stop on shutdown
  > Daytona auto-stops idle sandboxes server-side (auto_stop_interval), so aclose
  > stopping every cached sandbox raced the auto-stop and raised "Sandbox is not
  > started" once it had fired. Shutdown now just releases references.
- `2026-06-18` AIP-126: bake skill deps into the daytona sandbox image + date-stamped tag
  > Pre-install the libraries the vireox-all-agents-flat skills import (reportlab,
  > pypdf, jinja2, markupsafe, premailer, boto3, markitdown via pip; pptxgenjs on
  > the base image's nvm Node) so they are present without per-turn self-install,
  > which the 20s execute cap makes unreliable. Chromium/Playwright and LibreOffice
  > are intentionally omitted (documented in the Dockerfile) to keep the image lean.
  >
  > make sandbox-image now also pushes a unique 0.5.1-<date> tag: re-pushing the
  > fixed 0.5.1-user tag is a no-op the Daytona snapshot pins, so a new build never
  > reaches new sandboxes. A date-stamped tag lets a fresh snapshot pull new content.
- `2026-06-19` AIP-152: key run-now workflow id on the session_id
  > Use the run's session_id as the workflow id suffix (run-<task_id>-<session_id>) instead of a second random value, so one id correlates the Temporal workflow run and its history row.
- `2026-06-19` AIP-152: add get_scheduled_task_run detail tool + run_now returns session_id
  > New get_scheduled_task_run(task_id, session_id) tool drills into one run: status, start/finish, duration, error, and full todos (store.get_run, owner+task scoped). run_now now generates the run's session_id, passes it to the workflow (which still mints its own for scheduled runs), and returns it — so the agent can immediately reference and then detail the run it just triggered. Skill updated for 'show details of that run' / 'why did that run fail'. Adds tests for get_run, ownership scoping, and run_now's returned id.
- `2026-06-19` AIP-152: await Client.list_schedules() in startup reconcile (fix TypeError)
  > list_schedules is a coroutine returning an async iterator, so it must be awaited before 'async for'; startup reconcile raised TypeError ('async for' on a coroutine) and re-registered nothing. The unit-test fake had a sync list_schedules, hiding it — made the fake an async def to match the real client contract.
- `2026-06-19` AIP-152: make delete_schedule idempotent so delete/recreate survive a missing schedule (Bugbot)
  > delete_schedule raised if the Temporal schedule was already gone (prior reset / partial failure), so delete_task could never complete — the task stayed in the registry with no recovery path via tool or REST — and the cadence-change recreate path could wedge the same way. delete_schedule now treats RPCError NOT_FOUND as success (idempotent) and re-raises other errors (e.g. Temporal down). Adds 2 tests.
- `2026-06-19` AIP-152: apply cadence edits to Temporal even while paused (Bugbot)
  > Editing a paused task's cadence only wrote Postgres; _reconcile_schedule's pause branch returned without touching the Temporal cron, so resuming later fired the OLD cadence despite the registry showing the new one. Restructured _reconcile_schedule into two steps: (1) ensure a schedule with the current cron exists (recreate on a cadence change even while paused), then (2) align the schedule's paused-state with the status. A resume now unpauses an already-correct schedule. Adds a regression test.
- `2026-06-19` AIP-152: commit registry before reconciling Temporal in update_task (Bugbot)
  > update_task mutated Temporal (_reconcile_schedule) before store.update committed the cadence/status, so a failed Postgres write left the schedule ahead of the registry with nothing to repair it. Now the registry commits first (source of truth, matching create_task); a subsequent Temporal failure leaves the registry reflecting the request, and startup reconcile re-registers active recurring tasks from it. Strengthened the cadence-change failure test to assert the registry holds the new cadence.
- `2026-06-19` AIP-152: close run row on activity failure + impersonation header on OAuth MCP (Bugbot)
  > Two Cursor Bugbot findings on the prior commit:
  > - Run history stuck 'running': run_scheduled_turn_activity only recorded a finish on the normal return path, so a missing task row or a setup (sandbox/hydration) exception left the run row open on 'running' with misleading last-run metadata. It now closes the row as 'failed' (capturing the error) before re-raising, so Temporal still retries per the policy.
  > - Headless MCP skipped impersonation on OAuth servers: the vault-OAuth connection path didn't attach extra_headers, so a headless run on a per-user-OAuth server omitted the X-Authenticated-Email principal header (FR-15/D-035). It now attaches the principal header to OAuth connections too; the provider still owns Authorization.
  >
  > Adds 4 regression tests (2 per fix).
- `2026-06-19` AIP-152: reconcile verifies schedules exist in Temporal, not just the registry id (Bugbot)
  > Startup reconcile decided 'has a schedule' from the registry's temporal_schedule_id, so an active recurring task whose schedule vanished from Temporal (store reset under a populated registry — the exact case the docstring promised to cover) was skipped and never fired again. It now lists Temporal's live schedules once and re-registers any active recurring task whose schedule is actually missing, realigning the registry id either way; ScheduleAlreadyRunningError (eventual-consistency race) is treated as 'exists'. Adds a Temporal-reset regression test.
- `2026-06-19` AIP-152: make schedule writes fail safe into reconcile (Bugbot round 2)
  > Two partial-failure issues from Cursor Bugbot on the fix commit (Temporal + Postgres have no shared transaction, so ordering must fail toward what startup reconcile repairs):
  > - create_task now writes the registry row BEFORE creating the Temporal schedule, then persists the id. A schedule-create failure leaves an active+recurring row with no id (reconcile recreates it) instead of an orphan schedule firing for a non-existent row.
  > - _reconcile_schedule mirrors each Temporal op to the registry and clears temporal_schedule_id BEFORE recreating on a cadence change, so a failed recreate leaves no stale id pointing at a deleted schedule — reconcile recreates it.
  >
  > Adds 2 failure-injection tests.
- `2026-06-19` AIP-152: fix 5 schedule-reconcile bugs in update_task (Cursor Bugbot)
  > _reconcile_schedule now returns the schedule id to persist and handles all transitions:
  > - resume (active, unchanged cadence): unpause Temporal instead of no-op — schedule was left paused
  > - cadence→on-demand: delete the recurring schedule (was left firing) and clear the id
  > - on-demand→recurring: persist the new temporal_schedule_id so delete_task can clean it up
  > - recreate paths use the task's stored timezone (row.timezone), not the request's x-timezone
  > update_task enforces scheduled_task_max_per_user on a paused→active resume (NFR-8), matching create_task.
  >
  > Adds 5 regression tests with a fake Temporal client recording create/delete/pause/unpause.
- `2026-06-19` AIP-152: ruff-format scripts/scheduling_smoke.py (fix CI --all-files gate)
  > The script was committed with only 'ruff check --fix' applied, not 'ruff format'; CI runs pre-commit --all-files, which reformatted it (long argparse/print lines wrapped). Formatting-only.
- `2026-06-19` AIP-152: refine tool description for run history access
  > Updated the description in the build_scheduling_tools function to clarify that a run's todos and progress should be accessed through the list_scheduled_tasks function, enhancing the guidance for users on how to retrieve run history effectively.
- `2026-06-19` AIP-152: few-shot guidance for task instructions (prefer a reference example file)
  > writing-instructions.md gains a 'Few-shot: prefer a reference file over inline examples' section + a worked example: when output shape matters, write the sample(s) to /memory/user/scheduled_tasks/examples/<task>.md and have the instruction read it, keeping the instruction lean and the example reusable/editable. SKILL.md gets a one-line pointer. Synced to the spec asset.
- `2026-06-19` AIP-152: steer the agent to list_scheduled_task_runs for a run's todos
  > Agent answered 'show me the run's todos' by reading the instruction file + ls instead of calling list_scheduled_task_runs (run history). Make the tool description + skill explicit: a run's status/todos/progress live in the run-history tool, NEVER in the instruction file. (The run history itself works — the task had 3 successful runs with full todos captured.)
- `2026-06-19` AIP-152: bound record_run_started retries so a failing run can't loop forever
  > The recurring 'record_run_started missing session_id' spam was a pre-fix workflow execution replaying its old 2-arg history (terminated out-of-band). record_run_started used Temporal's default unlimited activity retry, so a poisoned/wedged bookkeeping activity retried indefinitely. Cap it at 3 attempts → the workflow fails instead of looping.
- `2026-06-19` AIP-152: add list_scheduled_task_runs tool (run history via the agent)
  > FR-18 said run history is available 'via the agent' but only REST exposed it; the agent had no tool, so 'show run history' didn't work. Add list_scheduled_task_runs(task_id) — returns each run's status, timing, error, and captured todos, owner-scoped — wired into the builder and documented in the skill (Tools + Managing).
- `2026-06-19` AIP-152: scheduling fixes — caller timezone, list-tool awareness, smoke script
  > - thread the caller's x-timezone into scheduled-task creation (was hardcoded UTC, so '10am' tasks fired at 10:00 UTC instead of the user's tz). chat.py resolves it; build() passes it to the scheduling tools. - manage-scheduled-tasks skill: trigger on list/status phrasing ('list my tasks', 'what's running', 'task status') and tell delegating/supervisor teams to handle scheduled-task requests DIRECTLY, not delegate to a subagent (subagents don't have the tools) — the supervisor wasn't calling list_scheduled_tasks. - add scripts/scheduling_smoke.py: live end-to-end scheduler smoke (immediate run or fast cron) that polls run history; used to self-test the feature.
- `2026-06-19` AIP-152: fix scheduled-task workflow→activity arg mismatch
  > record_run_started gained owner_email and the run activity now records its own outcome, but ScheduledTaskWorkflow.run still passed record_run_started 2 args and called a removed record_run_finished activity (TypeError at runtime; structural unit tests don't execute the workflow). Pass owner_email, drop the dead call. Add a Temporal test-server integration test that executes the workflow end-to-end so this class of mismatch is caught.
- `2026-06-19` Merge origin/main (AIP-165 #30) into AIP-152 scheduled-tasks branch
  > Auth-edge reconcile: AIP-165 added a userinfo email/name profile (RequestAuthContext.user_email/user_name); AIP-152 D-034 keys user_id on the verified email. Kept both — user_id is the lowercased email (D-034, fallback to sub in dev), user_email/user_name from the profile. Updated runtime/AGENTS.md (was 'user_id is sub') to match. 436 unit tests pass.
- `2026-06-19` AIP-152: scheduled agent tasks on Temporal
  > New runtime/scheduling/ subsystem for recurring, server-side agent runs (off by default via RUNTIME_SCHEDULER_ENABLED): Temporal scheduling (cadence/timezone, ALLOW_ALL overlap, exponential-backoff retry); Postgres scheduled_tasks registry + scheduled_task_runs history with live todos capture; agent tools + auto-injected manage-scheduled-tasks skill; headless run path reusing DeepRuntimeRunner with the instruction in a per-user memory file; email identity (D-034) + impersonation MCP auth (D-035); /v1/schedules REST + an in-process Temporal worker started in the lifespan. Spec: specs/009-scheduled-agent-tasks. verify.sh: 479 passed, 83% coverage.
- `2026-06-19` AIP-165: memory guide loads, episodic index discovery, user-profile prompt block (#30)
  > * AIP-165: memory guide loads, episodic index discovery, user-profile prompt block
  >
  > Memory (spec 008 follow-up):
  > - manage-memory SKILL.md: its description was invalid YAML (a `): ` colon-space),
  > so SkillsMiddleware silently dropped the whole skill and the agent lost its memory
  > map. Rewrite it as valid YAML carrying the type->location map (always in context):
  > semantic AGENTS.md, procedural skills/, episodic episodes/.
  > - episodic.py: the always-on "recent conversations" note now names
  > /memory/user/episodes/_index.md so the agent can discover and page the full episode
  > collection beyond the inline recent list (static pointer; cache-stable).
  >
  > User-profile prompt block:
  > …

## `vireox-runtime-plugin` — Vireox runtime plugin (19)

- `2026-06-15` README: document procedure parity (agent-body imperatives ported into skills)
  > Renames the parity section to Format/routing/procedure and adds the third
  > regression now closed — each skill carries a "Procedure (mandatory)" block
  > porting the original agent-body imperatives (friendly-score dual sources,
  > full-score weekly-OOS datasource, askdata canonical-match, marketing promo
  > explode/exclude, fresh-score sub_category trap, payroll workflow).
- `2026-06-15` Port agent-body procedures into flat skills; eval-skill grounding + tables-touched
  > Follow-up to 14e7a46 (AIP-158). The flat skills carried the original agents'
  > references but not their imperative procedures; ported the behavior-defining ones,
  > grounded in each original `vireox-all-agents/agents/<agent>.md`:
  >
  > - friendly-score: always query BOTH cx_survey_answers + cx_locations_reviews
  > (if surveys empty, review complaints are the answer); theming; verbatim buckets.
  > - full-score: weekly OOS via product_inventory_pricing (oos_days = 7 - days_in_stock),
  > snapshot only for on-hand/as-of-date; "previous week" = last complete Mon-Sun.
  > - askdata: canonical-match before "no data"; cross-dimension → ask; existence =
  > live filtered query; use metadata sql/sql_table, never assume columns.
  > - marketing-ai: default model transactions_order_items; promo field
  > order_level_discount_discounts_names → explode → exclude → rank + disclosure note.
  > …
- `2026-06-15` Harden vireox-all-agents-flat: canonical queries, context trims, CSV gate
  > Follow-up to bf4d7ee (AIP-158), driven by the E02 eval loop:
  >
  > - full-score: add canonical FULL + hot-SKU SQL (read the precomputed
  > full_score_summary; top-80% SKUs ⋈ inbound movements). Stops the key/hot-SKU
  > set drifting run-to-run (506→520→565, 509→403). Verified: run 4b0169d3 is back
  > to 506/58.10% and 509/29.67% = ground truth, and the fastest+cheapest run.
  > - data-agent-conventions: gate the CSV link to when a download helps (the tool
  > emits a csv_url on every result); read-only-what-you-need directive.
  > - store-profile: hard cap (compact + relevant-store only) and trim 447 -> 81 lines.
  > - coordinator-playbook/csv-and-presentation: align with the CSV gate.
  > - AGENTS.md: drop human/infra meta, fix the 'no subagents' wording (99 lines).
  > - evaluate-agent-run: per-turn token breakdown (input/output/cache_read/
  > …
- `2026-06-15` Add vireox-all-agents-flat (single-agent Deep Agents team) + eval harness
  > A/B candidate to replace the agno-era delegated team for the Deep Agents
  > rollout (AIP-158, sub of AIP-126). Stays Claude-plugin-compatible.
  >
  > - plugins/vireox-all-agents-flat/: one root agent, no delegated data agents;
  > the 12 subagents fold into 15 on-demand skills. Long-runtime harness:
  > report pipeline (build_report.py -> report.html -> report-verifier),
  > sensors (harness/sensors.py), PostToolUse hook. Golden eval set (evals/).
  > - .claude/skills/evaluate-agent-run/: reusable run-vs-ground-truth scorer
  > (accuracy / output format / TTFT / token usage).
  > - docs/subagents-vs-skills-analysis.md + harness best-practices docs.
  > - Output-format + routing parity fixes: always-on score-answer contract in
  > data-agent-conventions, mandatory score templates, enriched skill
  > …
- `2026-06-16` Pilot vireox-all-agents-flat (single-agent Deep Agents team) + eval harness [AIP-158] (#1)
  > * Add vireox-all-agents-flat (single-agent Deep Agents team) + eval harness
  >
  > A/B candidate to replace the agno-era delegated team for the Deep Agents
  > rollout (AIP-158, sub of AIP-126). Stays Claude-plugin-compatible.
  >
  > - plugins/vireox-all-agents-flat/: one root agent, no delegated data agents;
  > the 12 subagents fold into 15 on-demand skills. Long-runtime harness:
  > report pipeline (build_report.py -> report.html -> report-verifier),
  > sensors (harness/sensors.py), PostToolUse hook. Golden eval set (evals/).
  > - .claude/skills/evaluate-agent-run/: reusable run-vs-ground-truth scorer
  > (accuracy / output format / TTFT / token usage).
  > - docs/subagents-vs-skills-analysis.md + harness best-practices docs.
  > …
- `2026-06-16` Ground flat-team skills vs source+production; add eval + provenance suite
  > Skills (grounded against plugins/vireox-all-agents source + same-time production):
  > - friendly-score: point the agent to guide_friendly_score.txt "## Scoring Logic"
  > for the Friendly Score number (rating-based, Google-reviews-only). Fixes E01 T1
  > (70.00% sentiment+surveys -> 57.14% = production exact). Trimmed ungrounded
  > dual-source emphasis to source intent ("Always query both"); is_complain -> pointer.
  > - full-score: remove fabricated run-specific "drift counts"; re-ground the hot-SKU
  > restock-% query (E02 = production 29.67% exact) as reusable SQL only; remove the
  > dangling inventory_snapshot_policy.md reference (broken in source too).
  >
  > Harness:
  > - rename scenario-runner -> auto-eval-vireox-all-agents-flat (end-to-end: capture
  > prod GT -> run local -> score). run_scenario.py: --target aghub, --cookie-env,
  > …
- `2026-06-16` Add self-run scenario loop; verify friendly-score dual-source fix (E05 3/3)
  > - .claude/skills/run-agent-scenario/: run_scenario.py drives a threaded multi-turn
  > conversation at the local /librechat/v1/chat/completions (stream=true, bearer
  > from .env), under a fixed x-session-id (= trace thread_id) so the run is
  > immediately scoreable; captures answer prose (the trace doesn't) + E05 scenario.
  > - .claude/agents/scenario-runner.md: subagent that runs a scenario then scores it
  > with evaluate-agent-run end-to-end (read original agent body before suggesting).
  > - friendly-score: strengthen the dual-source rule — query BOTH survey + Google-
  > review sources in the SAME turn even when the user names only "surveys"; pivot
  > to reviews when the primary source is empty (required secondary feedback check).
  > - evals: E05 verification run (9261fa07) — 3/3 vs ground truth; complaints turn now
  > touches cx_survey_answers + cx_locations_reviews in one turn (surfaces 3 review
  > complaints, no follow-up); OOS category breakdown + promo disclosure also confirmed.
  > …
- `2026-06-17` docs: refresh captured system prompt with post-fix version [AIP-158]
  > Recapture latest-system-message.md from conversation 9de46d2c (10:16Z) after the
  > runtime prompt fixes landed: single identity, no leaked coding/monitoring base
  > content, consolidated clarifying guidance, and the report-builder routing live.
- `2026-06-17` docs: capture flat-team system prompt + prompt-engineering review [AIP-158]
  > - latest-system-message.md: assembled coordinator prompt, captured from a
  > runtime trace (AGENTS.md + runtime guidance + MCP instructions + deepagents
  > BASE_AGENT_PROMPT).
  > - system-prompt-review-fixes.md: layered source map + fixes. Key finding —
  > AGENTS.md is already the cleanest layer; most issues live in the deepagents
  > BASE_AGENT_PROMPT appended last, fixable via the runtime profile override.
  > - notification-agent-v4.md: source HTML-report instruction doc the report-builder
  > email-safe rules were distilled from.
- `2026-06-17` Add report capability to vireox-all-agents-flat [AIP-158]
  > Build → review → email-safe HTML export → deliver, with PDF/PPTX handoff.
  >
  > - report-builder skill: drafts a structured report and exports a self-contained
  > email-safe HTML file (Slate & Indigo palette; inline CSS, nested-table layout,
  > Roboto 16px, wide-table scroll). 3 responsive templates + html-template ref.
  > - report-reviewer agent: independent PASS/FAIL QA gate (structure, data
  > integrity, tone, disclaimers, email-safe HTML). The team's one intentional
  > subagent — a gate, not domain delegation — so the flat design holds.
  > - minimax-pptx: vendored from MiniMax-AI/skills (pptx-generator), renamed.
  > - wire plugin.json access gate (+report-builder, +minimax-pptx) and AGENTS.md
  > routing through build → review → deliver.
  > - send-email declared as tools:[send-email] forward reference; the native
  > …
- `2026-06-17` Drop report pipeline; add minimax-pdf/xlsx document skills
  > vireox-all-agents-flat:
  > - Remove the report pipeline: delete the report-builder skill and the
  > report-verifier subagent, and strip the HTML-report logic from harness
  > sensors, the PostToolUse hook, and the evals golden set (CSV-URL +
  > delegation/date sensors stay). Deliverable is now the chat answer with CSV
  > links; the team has no subagents. Fix the data-agent-conventions instruction
  > that pointed at the deleted skill; update README + plugin.json prose.
  > - Add the minimax-xlsx skill (Excel/CSV create/read/edit/validate, from
  > MiniMax-AI/skills) and vendor minimax-pdf; register both in the access gate.
  > Both need sandbox deps at runtime (pandas/openpyxl; LibreOffice for xlsx
  > recalc; reportlab/pdfplumber/pypdf for pdf).
  > - Tidy AGENTS.md intro.
  > …
- `2026-06-17` Reframe vireox-all-agents-flat root as conversational assistant; cut re-load duplication
  > Verified from a runtime trace that the root re-read the same SKILL.md +
  > semantic metadata every turn even though conversation history was preserved —
  > the prompt kept ordering re-loads. Fixes are prompt-only.
  >
  > - AGENTS.md: rewrite the "Root Analyst workflow" (Resolve->Clarify->Load->Present)
  > into a conversational Data Assistant; add a reuse-guard (don't re-read a skill /
  > reference / semantic metadata already in history); remove the routing table
  > (runtime auto-injects each skill's name+description every turn); remove the dead
  > {{current_datetime}}/{{current_agent_id}} placeholders (read verbatim, no
  > template engine); trim the sandbox-capability line the framework already injects.
  > - fast/fresh/friendly-score: soften "before answering, read template_X" to
  > "follow the layout in template_X" so it is not re-read each turn.
  > …
- `2026-06-18` Fix flat-team OOS week bucket + fresh-score dual-table; rename eval criterion to Time Duration [AIP-158]
  > Eval-driven fixes for vireox-all-agents-flat (found via E02/E04/E05 runs):
  >
  > - full-score: pin the canonical weekly-OOS query (oos_days_in_week =
  > GREATEST(0, 7 - SUM(days_in_stock)), HAVING > 0, window totals) and harden the
  > week-bucket guard to the last complete Monday-Sunday week. The prior E05 Turn-1
  > run selected the current incomplete week bucket and improvised the aggregation
  > (81/294 vs GT 112/379). Repointed the conflicting "Out-of-Stock Products" example
  > to the canonical query. Note: product_inventory_pricing and
  > unified_retail_metrics_products_out_of_stock are the same physical table.
  >
  > - fresh-score: require consulting BOTH freshness tables
  > (current_inventory_freshness_metrics for current on-hand,
  > …
- `2026-06-18` report-builder follow-ups: reuse collected data + keep scratch out of outputs [AIP-158] (#3)
  > * report-builder: reuse already-collected data on report turns [AIP-158]
  >
  > Fix: on a "generate a report from the data above" turn the agent re-queried
  > instead of reusing data gathered in earlier turns. Add a reuse-first rule to
  > SKILL.md Step 1 (skip collection when the conversation already has the data;
  > pull only what's missing) and a matching nudge in AGENTS.md.
  >
  >
  > * report-builder: tighten reuse-first wording [AIP-158]
  >
  > Trim the reuse-first rule in SKILL.md Step 1 and the AGENTS.md report bullet to
  > concise prompt wording.
  > …
- `2026-06-18` report-builder: keep pipeline scratch out of surfaced outputs [AIP-158]
  > The runtime auto-surfaces every workspace file the agent writes as a download
  > link (hydrate.collect), ignoring dot-files/dot-dirs. report-builder was leaking
  > its intermediates (insights.json, formatted.json, CSVs, content.json, the .j2).
  >
  > Do all pipeline work in a hidden .report/ dir (ignored) and write only the
  > deliverable report HTML to the workspace root:
  > - format_data.py default --outdir -> .report
  > - SKILL.md + references repoint every step path to .report/; add a "working files
  > vs deliverables" rule; document copying a CSV to root to offer it as a download.
- `2026-06-18` report-builder: tighten reuse-first wording [AIP-158]
  > Trim the reuse-first rule in SKILL.md Step 1 and the AGENTS.md report bullet to
  > concise prompt wording.
- `2026-06-18` report-builder: reuse already-collected data on report turns [AIP-158]
  > Fix: on a "generate a report from the data above" turn the agent re-queried
  > instead of reusing data gathered in earlier turns. Add a reuse-first rule to
  > SKILL.md Step 1 (skip collection when the conversation already has the data;
  > pull only what's missing) and a matching nudge in AGENTS.md.
- `2026-06-18` Add report capability to vireox-all-agents-flat [AIP-158] (#2)
  > * Add report capability to vireox-all-agents-flat [AIP-158]
  >
  > Build → review → email-safe HTML export → deliver, with PDF/PPTX handoff.
  >
  > - report-builder skill: drafts a structured report and exports a self-contained
  > email-safe HTML file (Slate & Indigo palette; inline CSS, nested-table layout,
  > Roboto 16px, wide-table scroll). 3 responsive templates + html-template ref.
  > - report-reviewer agent: independent PASS/FAIL QA gate (structure, data
  > integrity, tone, disclaimers, email-safe HTML). The team's one intentional
  > subagent — a gate, not domain delegation — so the flat design holds.
  > - minimax-pptx: vendored from MiniMax-AI/skills (pptx-generator), renamed.
  > - wire plugin.json access gate (+report-builder, +minimax-pptx) and AGENTS.md
  > …
- `2026-06-18` Rework report-builder: script pipeline, unified content.json, SES email [AIP-158]
  > Rebuild the report-builder skill into a deterministic, script-driven CREATE
  > pipeline whose content.json renders to BOTH HTML and PDF, and deliver via SES.
  >
  > - Pipeline: collect insights -> format_data.py (pretty CSVs + formatted.json)
  > -> content.json -> Jinja2 templates -> render_html.py -> email-safe HTML.
  > - Unified schema: content.json `blocks` use minimax-pdf's vocabulary
  > (h1/h2/h3, body, bullet, numbered, callout, table, chart, image, divider,
  > spacer, caption), so the same file renders HTML here and PDF via minimax-pdf
  > (no converter). HTML-only extras (csv_url, value_labels) and minimax-only
  > (col_widths) are ignored by the other side.
  > - Templates are a component library: base.html.j2 (shell + stylesheet),
  > components.html.j2 (macros), report.html.j2 (default layout). CSS is authored
  > …

## `100x-agent-hub` — Backend agent platform (3)

- `2026-06-16` fix(agents): break Responses chain on day change so datetime stays live (CENG-522) (#446)
  > The OpenAI Responses API stores conversation state server-side (store=True)
  > and reuses the chain ROOT's system message for every previous_response_id
  > link. Agno chains turns by sending only messages after the last assistant
  > response, so the freshly built system message (with add_datetime_to_context)
  > is sliced away and never sent. A conversation resumed on a later day keeps
  > getting the original day's clock, so the agent treats current weeks as "in
  > the future" while live data contradicts it.
  >
  > Fix: AgentHubOpenAIResponses strips response_id + reasoning_output from
  > assistant messages not dated today (user timezone) before each request, so
  > Agno re-sends the full context (today's system message) on the first turn of
  > a new day and OpenAI starts a fresh chain root. Same-day turns keep chaining,
  > …
- `2026-06-16` fix(agents): break Responses chain on day change so datetime stays live (CENG-522)
  > The OpenAI Responses API stores conversation state server-side (store=True)
  > and reuses the chain ROOT's system message for every previous_response_id
  > link. Agno chains turns by sending only messages after the last assistant
  > response, so the freshly built system message (with add_datetime_to_context)
  > is sliced away and never sent. A conversation resumed on a later day keeps
  > getting the original day's clock, so the agent treats current weeks as "in
  > the future" while live data contradicts it.
  >
  > Fix: AgentHubOpenAIResponses strips response_id + reasoning_output from
  > assistant messages not dated today (user timezone) before each request, so
  > Agno re-sends the full context (today's system message) on the first turn of
  > a new day and OpenAI starts a fresh chain root. Same-day turns keep chaining,
  > …
- `2026-06-17` Merge pull request #451 from 100xteam-ai/dev
  > fix(agents): break Responses chain on day change so datetime stays li…
