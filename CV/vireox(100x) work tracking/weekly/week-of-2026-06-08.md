# Week of 8 June 2026

> **Range:** Mon 8 Jun – Sun 14 Jun 2026  
> **Activity:** 76 commits across 5 repos · 18 Linear tickets

| Repository | Commits | Domain |
|---|---|---|
| `100x-agent-runtime` | 42 | Deep Agents runtime |
| `100x-agent-hub` | 13 | Backend agent platform |
| `100x-web-application` | 11 | Web app API |
| `100x-ai-plugins` | 5 | Plugin marketplace / SDD harness |
| `vireox-runtime-plugin` | 5 | Vireox runtime plugin |

---

## 🎫 Linear tickets

**Completed (14)**

- ✅ **AIP-127** — /models endpoint + multi-model agent config _(High)_
- ✅ **AIP-128** — Config repo + sync to deep agent runtime _(High)_
- ✅ **AIP-129** — File upload & download for users _(Medium)_
- ✅ **AIP-130** — OTLP export to SigNoz (observability & session tracing) _(Medium)_
- ✅ **AIP-143** — Parse x-knowledge-base-context: LibreChat KB integration for Harness teams _(Medium)_
- ✅ **AIP-146** — Runtime never restarts auto-stopped Daytona sandboxes _(High)_
- ✅ **AIP-147** — Route chat CSV/XLSX uploads to the sandbox Files API _(Medium)_
- ✅ **AIP-121** — Make /v1/chat/completions SSE stream OpenAI-client compatible _(Medium)_
- ✅ **AIP-123** — Stream tool-call, reasoning, and usage events _(Medium)_
- ✅ **AIP-77** — Support Claude Plugins runtime on our platform _(High)_
- ✅ **AIP-103** — Add components architecture diagram _(Medium)_
- ✅ **AIP-104** — Add data schema _(Medium)_
- ✅ **AIP-1** — [LLM] Report Generation Reliability — Background Mode & Timeout Handling _(Urgent)_
- ✅ **PENG-255** — Enable background mode for report generation _(High)_

**In progress / opened (3)**

- 🔵 **AIP-151** — Fix skills never loading in agent runtime _(Medium)_
- 🔵 **AIP-132** — Move workflow-definition generation to Temporal _(Medium)_
- 📋 **AIP-118** — Setup and test report creation on Harness runtime _(Backlog, Medium)_

**Canceled (1)**

- ❌ **AIP-131** — LibreChat config ACL (per-user agent access) _(Medium)_

---

## `100x-agent-runtime` — Deep Agents runtime (42)

- `2026-06-08` AIP-127: agent teams + /v1/models, brain/builder split, harness package
  > Expose each plugin folder as a selectable agent team via GET /v1/models and
  > route chat by team id (404 model_not_found on miss; raw LLM strings rejected —
  > clean break). One team per turn: AGENTS.md prompt + plugin.json model + its
  > subagents, with per-subagent skill/MCP access enforced. Teams sourced from the
  > system plugin root. Migrate vireox-askdata + co3-pipeline-copilot.
  >
  > Refactors (behaviour-preserving, 134 tests green):
  > - D-014: split the brain into a thin model loop (runtime/harness/brain) + an
  >   agent builder (runtime/agent) that assembles create_deep_agent; the brain
  >   streams a BuiltAgent and holds no assembly logic.
  > - D-015: group brain/sandbox/session/plugins/observability under
  >   runtime/harness/ (Agent = Model + Harness); supersedes D-008's layout.
  > …
- `2026-06-08` AIP-127: observability top-level + session-id header to ingress
  > Refine the harness grouping (D-015):
  > - observability is cross-cutting (instruments ingress + the whole request),
  >   so move runtime/harness/observability/ back to runtime/observability/.
  > - session-id resolution parses HTTP headers (ingress concern), so move
  >   runtime/harness/session/headers.py -> runtime/api/headers.py.
  >
  > harness/ now holds brain, sandbox, session, plugins. Imports, verify.sh,
  > docs, runtime/AGENTS.md, DECISIONS D-015, and spec plan updated. 134 tests green.
  >
  > Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
- `2026-06-08` AIP-127: docs — note api/headers.py in the ingress mapping
  > Final doc pass for the harness restructure: add runtime/api/headers.py to the
  > Ingress row of the component→harness mapping (session-id resolution).
  >
  > Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
- `2026-06-08` AIP-127: forward API bearer to MCP on the LibreChat route only (D-016)
  > LibreChat users should get plugin MCP tools without a per-user vault OAuth
  > connect. Make MCP-auth policy route-scoped at the interface:
  > - POST /librechat/v1/chat/completions: forward the caller's API bearer to MCP
  >   servers as the per-server fallback (api_bearer_for_mcp=True).
  > - POST /v1/chat/completions: stays strict — only when RUNTIME_MCP_USE_API_BEARER.
  > The agent builder now uses whatever bearer it is given (no internal policy gate).
  >
  > Tests: librechat forwards the token; openai route does not. Docs + DECISIONS
  > D-016 updated. 136 tests green.
  >
  > Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
- `2026-06-08` AIP-127: shorten MCP tool names to <server>_<tool> (D-017)
  > The model-visible MCP tool prefix is the connection key. Drop the redundant
  > plugin-name segment (a turn runs one team, so all loaded MCP tools already
  > belong to it): connection key = server name in both builders. FR-11 scoping in
  > build_subagent now applies the subagent allow/deny lists to the team pool
  > directly (no plugin-prefix filter). Rename the vireox server vireox-database ->
  > database, so tools read e.g. database_query_semantic_sql (matches the prompt).
  >
  > Updated mcp/agents/mcp_auth + .mcp.json/plugin.json/AGENTS.md + tests + docs
  > (D-017). 136 tests green; verified live: mcp_missing_auth now ['database'].
  >
  > Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
- `2026-06-08` AIP-127: plugin.json skills/mcpServers as team access gate (D-018)
  > Make the team's plugin.json `skills`/`mcpServers` an authoritative tri-state
  > access gate (they were parsed but never read before):
  > - key absent  -> None -> expose all (back-compatible)
  > - present []  -> expose none
  > - [names]     -> expose only those
  >
  > Stored as Plugin.skill_access / mcp_access (presence-aware parse). Enforced in
  > the builder: compute_team_skill_paths gates skill paths; load_mcp_tools gains
  > allowed_servers, threaded through both connection builders (mcp.py, mcp_auth).
  >
  > Tests for all three states (skills + MCP). 141 green. Verified live: vireox
  > with [] now exposes no skills and no MCP (mcp_missing_auth: None).
  > …
- `2026-06-09` AIP-127: docs — plugin/team folder structure authoring guide
  > Add docs/guides/plugin-structure.md: the on-disk contract for a team/plugin
  > folder — folder=team (id=folder name), .claude-plugin/plugin.json (model +
  > skills/mcpServers access gate, D-018), AGENTS.md (team prompt), agents/<name>.md
  > (subagents + per-agent skill/MCP allow/deny), skills/<name>/SKILL.md, .mcp.json
  > (<server>_<tool> naming D-017, vault/librechat auth D-016). Linked from guides
  > README, 03-tools.md, and the root AGENTS.md task table.
  >
  > Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
- `2026-06-09` AIP-128: Claude-compatible plugin access model + subagent streaming
  > Make plugins load+run in both Claude Code and this runtime, fix
  > per-agent access, MCP tool naming, and subagent streaming.
  >
  > - Manifest: read team model + access from x-agent-hub vendor key
  >   (D-020); top-level skills/mcpServers stay Claude component-path
  >   fields. Legacy top-level still read as a fallback. Migrate bundled
  >   plugins to x-agent-hub.
  > - Access is per-agent, no team gate (D-018/D-019): the root agent uses
  >   its own x-agent-hub.access; each subagent uses its own frontmatter,
  >   independent. All tri-state: absent -> all, [] -> none, [names] -> those
  >   (loader distinguishes a missing key from []). The builder opens
  >   connections for every server any agent references, then binds each
  > …
- `2026-06-09` AIP-128: fix subagent tool-call collision in pending map (Bugbot)
  > With subgraphs=True, parent and subagent number their tool-call indices
  > independently (both from 0), so keying pending_tool_calls by index alone
  > let a subagent's MCP call overwrite the parent's `task` entry at the same
  > index — corrupting args, suppressing the subagent call's announcement,
  > and mispairing/duplicating the task result.
  >
  > Key accumulation by (subgraph namespace, index) so parent and subagent
  > calls stay separate. Add a regression test covering both at index 0.
  >
  > Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
- `2026-06-09` AIP-128: mirror Cursor rules as Claude Code skills
  > Add Claude-side projections of the path-scoped Cursor rules so the same
  > guidance reaches Claude Code / Codex (AGENTS.md header: "For Cursor,
  > Claude Code, Codex"):
  >
  > - security-sensitive  <- .cursor/rules/030 (corrected stale src/** -> runtime/ paths)
  > - python-conventions  <- .cursor/rules/010
  > - docstrings-and-comments <- .cursor/rules/050 (pairs with the no-history pre-commit hook)
  >
  > Always-on rules (000 project-context, 040 SDD) intentionally stay in
  > AGENTS.md / the existing SDD skills, not duplicated as skills.
  >
  > Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
- `2026-06-09` AIP-128: only announce ancestor tool calls, not the subagent's own (Bugbot)
  > _announce_pending_calls fired on every subagent chunk and announced every
  > complete (id+name) pending call — including the subagent's OWN MCP call
  > whose args were still streaming across chunks, emitting truncated JSON and
  > marking it announced so clients never saw the full arguments.
  >
  > Announce only ANCESTOR calls (namespace is a strict prefix of the current
  > subagent's namespace) — e.g. the parent `task` call. The subagent's own
  > calls pair at their ToolMessage with complete args, as before. Add a
  > regression test for streaming-args.
  >
  > Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
- `2026-06-10` AIP-129: user file upload/download + per-user sandboxes
  > - OpenAI Files API surface (/v1/files): upload, list, metadata, content,
  >   delete; per-user ownership with 404 parity, optional session scoping,
  >   409 on same-scope filename conflict, 50 MiB default cap
  > - Postgres user_files metadata (origin=upload|output is the source of
  >   truth) + filesystem byte store under RUNTIME_FILES_DATA_DIR
  > - Per-turn workspace hydration via Deep Agents backend primitives:
  >   delta sync in, record-driven collect out (outputs become downloadable
  >   and rejoin the next turn); workspace dir auto-created; anchored at
  >   RUNTIME_FILES_WORKSPACE_ROOT or the backend work dir
  > - Per-user Daytona sandbox provider (D-023, partial D-006): lazy
  >   provision, label-based rediscovery across app restarts, auto-stop on
  >   idle, stop-all at shutdown; local-shell stays shared (dev/E2E)
  > …
- `2026-06-10` AIP-129: harden file workspace paths + delete propagation (Bugbot)
  > - Reject path-like upload filenames at the API edge (separators,
  >   traversal, control chars → 400); hydration skips any stored record
  >   whose filename is not a plain name (defense in depth)
  > - _safe_session_dir is injective: ids changed by sanitization or
  >   truncation get a hash suffix so distinct sessions cannot share a
  >   workspace dir
  > - hydrate removes previously-synced workspace paths whose record no
  >   longer exists, so DELETE /v1/files propagates to the sandbox before
  >   the next turn (FR-10) and a re-upload of the same name re-syncs
  >
  > Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
- `2026-06-10` AIP-129: record delete-propagation edge in plan (Bugbot follow-up)
  > Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
- `2026-06-10` AIP-129: inline file_data parts (Agent Hub parity) + per-type UC mapping
  > - ChatMessage.inline_files(): file parts carrying base64 file_data
  >   (or Agent-Hub-compatible fileData, optional data: prefix) are
  >   persisted as session uploads before hydration — idempotent for
  >   resent conversation history, content updates in place
  > - FileRegistry.find_by_scope_filename for the idempotent lookup
  > - Guide: split LibreChat use cases per type (PDF / CSV-TXT / Office),
  >   corrected to current fork behavior — x-*-context headers are legacy
  >   and inactive for custom endpoints, so document attaches deliver
  >   nothing to the runtime until the webchat forwards files
  >
  > Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
- `2026-06-10` AIP-129: correct LibreChat doc mapping — code-execution context is the live path for data files
  > Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
- `2026-06-10` AIP-129: doc — webchat routes attachments by file type (team-confirmed)
  > Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
- `2026-06-10` AIP-129: doc — source-verified LibreChat attachment routing (file:line refs)
  > Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
- `2026-06-10` AIP-129: record decided LibreChat integration design (KB header reuse, CSV/XLSX direct upload)
  > Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
- `2026-06-11` AIP-129: harden hydration sync + ingest + user-id keys (Bugbot round 2)
  > - sanitize_user_id is injective: ids altered by sanitization/truncation
  >   get a digest suffix, so distinct users can never share a sandbox,
  >   vault dir, or file dir (clean OIDC subs map to themselves, unchanged)
  > - hydrate verifies workspace presence with one listing per turn: a
  >   cache hit whose file was removed inside the sandbox re-uploads
  >   instead of skipping
  > - snapshot (and the workspace prompt note) now advertises only files
  >   verified present or confirmed uploaded — records with missing durable
  >   bytes are no longer announced to the agent
  > - ingest_inline_files never overwrites origin=output records (resent
  >   history must not clobber agent artifacts) and handles same-scope
  >   FileConflictError races by dropping the just-written bytes instead of
  > …
- `2026-06-11` AIP-129: File upload & download for users (+ per-user sandboxes) (#15)
  > * AIP-129: user file upload/download + per-user sandboxes
  >
  > - OpenAI Files API surface (/v1/files): upload, list, metadata, content,
  >   delete; per-user ownership with 404 parity, optional session scoping,
  >   409 on same-scope filename conflict, 50 MiB default cap
  > - Postgres user_files metadata (origin=upload|output is the source of
  >   truth) + filesystem byte store under RUNTIME_FILES_DATA_DIR
  > - Per-turn workspace hydration via Deep Agents backend primitives:
  >   delta sync in, record-driven collect out (outputs become downloadable
  >   and rejoin the next turn); workspace dir auto-created; anchored at
  >   RUNTIME_FILES_WORKSPACE_ROOT or the backend work dir
  > - Per-user Daytona sandbox provider (D-023, partial D-006): lazy
  > …
- `2026-06-11` AIP-143: parse x-knowledge-base-context for LibreChat KB integration
  > The LibreChat route now parses the x-knowledge-base-context header into
  > validated {knowledge_base_id, document_id, name} references (malformed
  > input is tolerated and behaves as no attachments), names the documents
  > in the system prompt, and forwards the normalized header to the team's
  > plugin MCP servers so the conversation-KB MCP tool scopes retrieval to
  > those documents. The header carries ids only; MCP auth stays on the
  > existing credential path (D-016) and context headers can never override
  > Authorization.
  >
  > Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
- `2026-06-11` AIP-143: register conversation-KB MCP as a runtime-internal server (D-024)
  > The conversation-KB MCP is first-party infrastructure, not team tooling:
  > when RUNTIME_CONVERSATION_KB_MCP_URL is set and the route forwards an
  > API bearer (LibreChat, D-016), the builder registers a conversation-kb
  > connection on every build — no team .mcp.json entry needed. The
  > x-knowledge-base-context header now rides only on that connection
  > instead of every plugin server, and its tools always bind to the root
  > agent (the D-018 gate governs team-declared servers only). Without the
  > header the KB server short-circuits to empty results (lazy activation).
  >
  > Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
- `2026-06-11` AIP-143: document RUNTIME_CONVERSATION_KB_MCP_URL in .env.example
  > Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
- `2026-06-11` AIP-143: cut chat-completion latency on the MCP load path
  > Register the internal conversation-kb server only on turns whose request
  > carries x-knowledge-base-context: the agent is rebuilt every turn, so a
  > mid-conversation attachment registers it on the next request, and turns
  > without attachments pay no MCP initialize/tools-list roundtrip. Load all
  > MCP servers concurrently so the per-turn cost is the slowest server, not
  > the sum. Tests pin RUNTIME_CONVERSATION_KB_MCP_URL so a developer .env
  > cannot leak into assertions.
  >
  > Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
- `2026-06-11` AIP-143: cache MCP tool specs per server; drop tool inventory from prompt (D-025)
  > PluginMcpClient.load_tools now fetches each server's tools/list once per
  > process and rebuilds LangChain tools from the cached specs bound to the
  > current request's connection — the cache holds name/description/schema
  > only, never headers or credentials, so nothing leaks across users. A
  > spec-cache hit costs no network roundtrip; a cached server that went
  > down fails at tool-call time instead of build time. Assumes per-server
  > tool lists are immutable across requests (restart to pick up changes).
  >
  > The root system prompt no longer lists MCP tool names or subagent names:
  > the model already sees both via its tool definitions and the deepagents
  > task middleware. The only MCP prompt line left is the vault-OAuth
  > missing-auth hint.
  > …
- `2026-06-11` AIP-143: deep-copy cached specs per request; document MCP loading model
  > Built tools reference the spec's schema dict, so serving the cached spec
  > by reference would let one request's mutation corrupt every later
  > request's tools; each conversion now gets its own deep copy (tested:
  > distinct args_schema per request). New guide docs/guides/mcp-tool-loading.md
  > covers the three layers — process-lifetime credential-free spec cache,
  > per-request connections with the credential resolution order, and the
  > fresh-session-per-call flow that keeps user A and user B isolated on the
  > same server.
  >
  > Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
- `2026-06-11` AIP-143: inject MCP server instructions into agent context
  > The initialize result's instructions field was silently dropped; it is
  > the server author's usage contract (the conversation-KB server ships a
  > routing guide there). Instructions are cached with the tool specs in the
  > per-server metadata cache (D-025, same immutability assumption) and
  > injected as an '# MCP server instructions' prompt section scoped to the
  > agents that hold that server's tools: the root prompt carries
  > root-bound servers' guidance, each subagent's prompt carries its own
  > servers' guidance. load_mcp_tools now returns an McpLoadResult dataclass
  > instead of a growing tuple.
  >
  > Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
- `2026-06-11` AIP-143: append signed public download links for produced files (D-026)
  > When RUNTIME_FILES_LINK_SECRET is set, the chat stream collects the
  > turn's new output files at StreamStop and appends a 'Files produced:'
  > markdown block of [filename](/v1/files/public/<token>) links to the
  > assistant message. The token is an HMAC-SHA256 capability over
  > (user_id, file_id, expires_at) with a configurable TTL (default 900s),
  > so the link downloads in any browser without an Authorization header;
  > every defect is a uniform 404. Errored turns keep the collect-in-finally
  > path without links. Secret unset = feature off, behavior unchanged.
  >
  > Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
- `2026-06-11` AIP-143: steer the model away from dead sandbox:/ links
  > The model writes [name](sandbox:/path) links by habit (code-interpreter
  > style); no client can open them. The workspace note now tells it never
  > to write file links itself — with download links enabled (D-026) the
  > runtime appends real signed links to the reply; without them it should
  > just name the file.
  >
  > Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
- `2026-06-11` AIP-143: address Bugbot review — collect retry + KB note gating
  > High: 'collected' was flipped before hydration.collect ran, so a collect
  > failure at StreamStop skipped the finally retry and lost the turn's
  > files; the flag now sets only after a successful collect, the failure is
  > swallowed (turn still ends cleanly, links lost) and finally retries.
  >
  > Medium: the KB note and runtime_meta.kb_documents applied whenever the
  > header parsed, even when the conversation-kb tools never loaded — the
  > prompt pointed the model at tools it could not call. Both are now gated
  > on the KB tools actually being bound.
  >
  > Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
- `2026-06-11` AIP-143: Parse x-knowledge-base-context — LibreChat knowledge-base integration (#16)
  > * AIP-143: parse x-knowledge-base-context for LibreChat KB integration
  >
  > The LibreChat route now parses the x-knowledge-base-context header into
  > validated {knowledge_base_id, document_id, name} references (malformed
  > input is tolerated and behaves as no attachments), names the documents
  > in the system prompt, and forwards the normalized header to the team's
  > plugin MCP servers so the conversation-KB MCP tool scopes retrieval to
  > those documents. The header carries ids only; MCP auth stays on the
  > existing credential path (D-016) and context headers can never override
  > Authorization.
  >
  > Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
  > …
- `2026-06-11` AIP-146: refresh Daytona sandbox state before restart check
  > Daytona auto-stops idle sandboxes server-side, but _ensure_started read
  > the state from the cached SDK object — a snapshot from provision time
  > that still said "started". The restart was skipped and the next toolbox
  > call failed with "failed to resolve container IP ... Is the Sandbox
  > started?". Call refresh_data() before deciding whether to start.
  >
  > Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
- `2026-06-11` AIP-146: refresh Daytona sandbox state before restart check (#17)
  > Daytona auto-stops idle sandboxes server-side, but _ensure_started read
  > the state from the cached SDK object — a snapshot from provision time
  > that still said "started". The restart was skipped and the next toolbox
  > call failed with "failed to resolve container IP ... Is the Sandbox
  > started?". Call refresh_data() before deciding whether to start.
  >
  > Co-authored-by: Claude Fable 5 <noreply@anthropic.com>
- `2026-06-12` AIP-147: allow duplicate filenames per scope; user-global files attach by file_id only
  > Two file-semantics changes for the webchat sandbox-upload flow (D-027, D-028):
  >
  > - D-027: hydrate() no longer sweeps user-global files into every turn; a
  >   file uploaded without a session header reaches a workspace only when a
  >   chat message attaches it by file_id (FR-13 file part).
  > - D-028: drop per-scope filename uniqueness (OpenAI Files semantics).
  >   POST /v1/files never 409s; every upload mints a new file_id. Hydration
  >   resolves a filename to the newest created_at record; older versions stay
  >   listable/downloadable by id. setup() drops the old unique indexes so
  >   deployed databases converge.
  >
  > LibreChat uploads attachments user-global (no conversation id at attach
  > …
- `2026-06-12` AIP-147: log the raw last user message on the LibreChat route + configure app logging
  > - runtime/api/chat.py: log the last user message as received on the wire
  >   (content parts included, long strings truncated) at the top of
  >   POST /librechat/v1/chat/completions — debug aid for attachment issues
  >   (shows whether the client sent `file` parts, spec KI-1). Log-only,
  >   never a span attribute.
  > - runtime/main.py: logging.basicConfig at import time. Uvicorn configures
  >   only its own loggers, so runtime.* INFO logs never reached the console.
  >   Logs go to stderr — stdout stays reserved for the OTLP-console exporter
  >   (Vector scrape).
  >
  > Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
- `2026-06-12` AIP-147: inline ingest never rewrites a newer duplicate in place
  > Bugbot finding on #20: ingest resolved a name to the newest record and
  > rewrote its bytes when the sha differed, so a replayed history part
  > carrying an older version could clobber the newest upload's content.
  >
  > Inline ingest now follows D-028 like every other upload path: content
  > already stored under the scope+filename (any version) is a no-op; new
  > content inserts a new record (newest wins hydration); in-place rewrites
  > are gone. find_by_scope_filename becomes list_by_scope_filename
  > (newest first).
  >
  > Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
- `2026-06-12` AIP-147: allow duplicate filenames per scope; user-global files attach by file_id only (#20)
  > * AIP-147: allow duplicate filenames per scope; user-global files attach by file_id only
  >
  > Two file-semantics changes for the webchat sandbox-upload flow (D-027, D-028):
  >
  > - D-027: hydrate() no longer sweeps user-global files into every turn; a
  >   file uploaded without a session header reaches a workspace only when a
  >   chat message attaches it by file_id (FR-13 file part).
  > - D-028: drop per-scope filename uniqueness (OpenAI Files semantics).
  >   POST /v1/files never 409s; every upload mints a new file_id. Hydration
  >   resolves a filename to the newest created_at record; older versions stay
  >   listable/downloadable by id. setup() drops the old unique indexes so
  >   deployed databases converge.
  > …
- `2026-06-12` AIP-151: fix skills never loading (sandbox seeding, source paths, virtual root)
  > Three stacked bugs left every agent skill-less:
  >
  > 1. Daytona sandboxes had no skill files. SkillsMiddleware reads SKILL.md
  >    through the agent's backend, but nothing copied plugin skill trees into
  >    the per-user sandbox. DaytonaSandboxProvider now seeds them on first
  >    get() per user (runtime/harness/sandbox/bootstrap.py, D-029); failed
  >    seeds log and retry next turn.
  >
  > 2. Named skill access lists (root gate D-018, subagent frontmatter D-019)
  >    mapped to per-skill directories, but SkillsMiddleware sources must be
  >    collection directories - any agent naming its skills loaded zero.
  >    Access now resolves to a SkillSelection value object (whole skills/
  > …
- `2026-06-12` Merge remote-tracking branch 'origin/main' into thuan/aip-151-fix-skills-never-loading-in-agent-runtime-sandbox-seeding
  > # Conflicts:
  > #	runtime/agent/deep_agent_builder.py
- `2026-06-12` Refactor logging in collect_skill_files to improve readability. Consolidated warning message for skipped skill files outside plugin root into a single line.
- `2026-06-12` AIP-151: fix skills never loading in agent runtime (#22)
  > * AIP-151: fix skills never loading (sandbox seeding, source paths, virtual root)
  >
  > Three stacked bugs left every agent skill-less:
  >
  > 1. Daytona sandboxes had no skill files. SkillsMiddleware reads SKILL.md
  >    through the agent's backend, but nothing copied plugin skill trees into
  >    the per-user sandbox. DaytonaSandboxProvider now seeds them on first
  >    get() per user (runtime/harness/sandbox/bootstrap.py, D-029); failed
  >    seeds log and retry next turn.
  >
  > 2. Named skill access lists (root gate D-018, subagent frontmatter D-019)
  >    mapped to per-skill directories, but SkillsMiddleware sources must be
  > …

## `100x-agent-hub` — Backend agent platform (13)

- `2026-06-08` fix(report): run workflow-definition generation in OpenAI background mode (PENG-255)
  > The "generate workflow definition" step built the report_workflow agent
  > (gpt-5.4, Responses API, 128k output) and ran it via streaming
  > (arun_stream_to_run_output). Streaming holds one HTTP connection open for
  > the whole long generation, so it failed on the upstream ~15-min request
  > timeout / network drops — the failure behind PENG-253.
  >
  > Agno's streaming path strips background=True (ainvoke_stream warns and
  > ignores it); only the non-streaming arun submits-and-polls. So enabling
  > background mode requires switching off streaming.
  >
  > - _enable_background_mode: flip background + poll/deadline on the agent's
  >   OpenAIResponses model (reuses REPORT_V3_* knobs; safe no-op otherwise).
  > …
- `2026-06-08` fix(report): address Bugbot review on workflow background mode (PENG-255)
  > - Bug 1 (content fallback): mirror the V3 path and fall back to
  >   response.content when response.messages is empty, so a successful run
  >   is not misread as an IndexError / invalid response.
  > - Bug 2 (duplicate background jobs): drop the app-level retry wrapper.
  >   Retrying agent.arun fires a fresh responses.create and starts a
  >   duplicate long-running generation instead of resuming the accepted job
  >   (Agno's poll loop propagates transient retrieve errors). Use a plain
  >   non-streaming arun, matching the AIP-1 V3 precedent; the caller already
  >   marks the workflow failed and alerts on error.
  >
  > Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
- `2026-06-08` docs(report): add scheduled report generation full-flow doc (PENG-255)
  > End-to-end reference for the scheduled report path: the two lifecycles
  > (API-driven workflow-definition build vs. Mage-driven recurring send),
  > the Mage cron pipeline (load_user_notifications_v2 -> send_notifications_v2),
  > the per-queue send script, UserReportGenerator's insights -> report -> email
  > steps, background-mode call sites, status/state in Mongo, retries/alerts,
  > manual/debug runs, and a component map.
  >
  > Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
- `2026-06-08` fix(report): background mode for workflow-definition generation (PENG-255) (#439)
  > * fix(report): run workflow-definition generation in OpenAI background mode (PENG-255)
  >
  > The "generate workflow definition" step built the report_workflow agent
  > (gpt-5.4, Responses API, 128k output) and ran it via streaming
  > (arun_stream_to_run_output). Streaming holds one HTTP connection open for
  > the whole long generation, so it failed on the upstream ~15-min request
  > timeout / network drops — the failure behind PENG-253.
  >
  > Agno's streaming path strips background=True (ainvoke_stream warns and
  > ignores it); only the non-streaming arun submits-and-polls. So enabling
  > background mode requires switching off streaming.
  >
  > …
- `2026-06-08` Merge pull request #435 from 100xteam-ai/dev
  > sync dev
- `2026-06-10` fix(chat): surface friendly message for OpenAI usage-policy rejections (AIP-79)
  > The agno TeamRunEvent.run_error handler discarded the real underlying
  > error and always emitted "TeamRunError - <model> stopped responding".
  > When OpenAI's safety filter rejected a prompt ("Invalid prompt: your
  > prompt was flagged as potentially violating our usage policy"), users
  > saw only the generic crash text with no idea what happened or what to do.
  >
  > - Read the real provider error from RunErrorEvent.content / error_type.
  > - Map known provider rejections (usage-policy / invalid-prompt) to a
  >   clear, actionable message via a new provider_error_messages helper.
  > - Log the raw provider error + error_type for debugging, even when the
  >   user sees the friendly message.
  > - Unknown errors keep the existing "stopped responding" fallback.
  > …
- `2026-06-10` fix(chat): shorten usage-policy user message (AIP-79)
  > Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
- `2026-06-10` fix(chat): handle OpenAI usage-policy rejection gracefully (AIP-79) (#441)
  > * fix(chat): surface friendly message for OpenAI usage-policy rejections (AIP-79)
  >
  > The agno TeamRunEvent.run_error handler discarded the real underlying
  > error and always emitted "TeamRunError - <model> stopped responding".
  > When OpenAI's safety filter rejected a prompt ("Invalid prompt: your
  > prompt was flagged as potentially violating our usage policy"), users
  > saw only the generic crash text with no idea what happened or what to do.
  >
  > - Read the real provider error from RunErrorEvent.content / error_type.
  > - Map known provider rejections (usage-policy / invalid-prompt) to a
  >   clear, actionable message via a new provider_error_messages helper.
  > - Log the raw provider error + error_type for debugging, even when the
  > …
- `2026-06-10` feat(report): move workflow-definition generation to Temporal (AIP-132)
  > Workflow-definition generation is a long single LLM call that can run
  > >25 min. It ran as a Celery task whose 25-min soft / 30-min hard limits
  > (colliding with the 1500s background-mode deadline from PENG-255)
  > truncated legitimate long runs with SoftTimeLimitExceeded / hard-kill.
  >
  > Move the job runtime Celery -> Temporal, following the auto-evals
  > worker/activity/workflow precedent:
  >
  > - Add CreateWorkflowDefinitionWorkflow + create_workflow_definition_activity
  >   on a new task queue (TEMPORAL_WORKFLOW_DEF_TASK_QUEUE). 40-min activity
  >   start_to_close_timeout (>= 1500s background deadline + margin), 2-min
  >   heartbeat, no retry (a retry would duplicate the long background run).
  > …
- `2026-06-10` feat(report): move run_workflow to Temporal + 2h timeouts (AIP-132)
  > Follow-up to moving workflow-definition generation: also move the
  > "run a workflow by ID" job runtime Celery -> Temporal, and raise both
  > job ceilings to 2 hours.
  >
  > - Add RunWorkflowWorkflow + run_workflow_activity on a new task queue
  >   (TEMPORAL_WORKFLOW_RUN_TASK_QUEUE), registered as a fourth worker.
  > - POST /report/workflow/{id}/run now starts the Temporal workflow; the
  >   202 + session_id contract is unchanged (task_id now carries the
  >   Temporal workflow ID instead of the Celery task ID).
  > - run_workflow_activity not retried at the Temporal layer (run_workflow
  >   already retries internally; ValueError is non-retryable).
  > - Set activity start_to_close_timeout and workflow run_timeout to 2h for
  > …
- `2026-06-10` fix(report): encrypt user token for Temporal + widen run_timeout (AIP-132)
  > Address PR review (Cursor Bugbot):
  >
  > - High: the route sent a plain token via model_dump(mode="json") while the
  >   Temporal activity rebuilds with user_info_from_dict, which Fernet-decrypts
  >   the token by default — decrypt fails on a plain JWT, the activity errors
  >   before the orchestrator runs, and the workflow doc stays pending after a
  >   201. Switch both routes to user_info_to_dict (encrypt on the way out) to
  >   match the activity's decrypt, mirroring the auto-evals Temporal path. This
  >   also keeps the bearer token out of Temporal's plaintext workflow history.
  > - Medium: run_timeout (2h) equalled the activity start_to_close_timeout (2h),
  >   so queue wait before the activity starts could terminate a legitimate run.
  >   Widen run_timeout to 2h30m so the activity keeps its full 2h work budget.
  > …
- `2026-06-11` feat(report): move workflow-definition generation to Temporal (AIP-132) (#442)
  > * feat(report): move workflow-definition generation to Temporal (AIP-132)
  >
  > Workflow-definition generation is a long single LLM call that can run
  > >25 min. It ran as a Celery task whose 25-min soft / 30-min hard limits
  > (colliding with the 1500s background-mode deadline from PENG-255)
  > truncated legitimate long runs with SoftTimeLimitExceeded / hard-kill.
  >
  > Move the job runtime Celery -> Temporal, following the auto-evals
  > worker/activity/workflow precedent:
  >
  > - Add CreateWorkflowDefinitionWorkflow + create_workflow_definition_activity
  >   on a new task queue (TEMPORAL_WORKFLOW_DEF_TASK_QUEUE). 40-min activity
  > …
- `2026-06-11` Merge pull request #443 from 100xteam-ai/dev
  > sync dev

## `100x-web-application` — Web app API (11)

- `2026-06-12` feat: AIP-147 route chat CSV/XLSX uploads to the sandbox Files API
  > On custom endpoints with sandbox: true + sandboxFileUploadUrl (librechat.yaml),
  > tabular chat attachments upload to the sandbox runtime Files API instead of the
  > Code Interpreter, and attach to each turn as OpenAI file content parts. The
  > sandbox server owns conflict handling; deleting the attachment removes the
  > sandbox copy. Flag off keeps Agent Hub and the Agents endpoint unchanged.
  >
  > Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
- `2026-06-12` build: AIP-147 bump @grpc/grpc-js to patch CVE-2026-48068/48069
  > New advisory (GHSA-5375-pq7m-f5r2, GHSA-99f4-grh7-6pcq) published after
  > main's last green Security Scan; it fails npm-audit-high and the Trivy
  > dependency scan on every new PR. Lockfile-only: @grpc/grpc-js 1.9.15
  > -> 1.9.16 and 1.14.3 -> 1.14.4 (nested under the OpenTelemetry gRPC
  > exporters), plus npm's dedup pruning of redundant nested entries.
  >
  > Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
- `2026-06-12` test: AIP-147 mock SandboxFileSync in processFiles spec
  > The spec replaces librechat-data-provider with a partial mock; the new
  > SandboxFileSync require chain pulls in the Agent schema, which needs the
  > real Visibility enum. Mocking the sync module cuts the chain (same fix
  > as process.spec.js).
  >
  > Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
- `2026-06-12` fix: AIP-147 address Bugbot findings on sandbox file parts
  > - Re-sent history / forked conversations: sandbox file parts are now
  >   also added to prior turns via processAttachments (resendFiles), so a
  >   forked conversation's fresh runtime session re-hydrates attachments.
  >   Shared addSandboxFileParts helper on BaseClient; no-op unless the
  >   endpoint sets options.sandbox.
  > - Token counting: sandbox attachments (no width/height) skip the image
  >   token loop instead of adding NaN to tokenCount.
  >
  > Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
- `2026-06-12` fix: AIP-147 npm 10 lockfile + name handling on file-part messages
  > - Regenerate the @grpc/grpc-js bump with npm 10 (CI's npm): the previous
  >   lockfile was written by npm 11, which prunes nested dedup entries that
  >   npm 10's npm ci requires (Missing rimraf/glob errors failed every job).
  >   Diff is now the two version bumps only.
  > - Bugbot follow-up: user messages carrying sandbox file parts now fall
  >   through to name handling instead of returning early, so they match
  >   plain user messages (vision early-return behavior unchanged).
  >
  > Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
- `2026-06-12` build: AIP-147 bump remaining nested @grpc/grpc-js copies to 1.14.4
  > npm 10's update only bumped the hoisted copy; three nested entries under
  > the OpenTelemetry gRPC exporters stayed at 1.14.3 and kept failing
  > npm-audit-high and the Trivy scan. npm audit (lock-only) now reports
  > 0 high.
  >
  > Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
- `2026-06-12` fix: AIP-147 KI-1 pass sandbox flag to AgentClient
  > Custom-endpoint chats run through AgentClient, not OpenAIClient, so the
  > sandbox file-part injection never fired on the wire (spec KI-1; runtime
  > D-027 removed the user-global sweep that masked it). agents/initialize
  > now passes the per-endpoint sandbox flag; the existing chain
  > (processAttachments -> addSandboxFileParts -> formatMessage ->
  > formatAgentMessages -> langchain verbatim passthrough) does the rest.
  > Regression tests on initialize plumbing and addSandboxFileParts.
  >
  > Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
- `2026-06-12` fix: AIP-147 KI-1 part 2 merge sandbox file_parts in AgentClient
  > The agents path formats messages with formatMessage/formatAgentMessages
  > from the @librechat/agents package, not the local prompts module — so
  > the local file_parts merge never ran and the part still missed the wire
  > even with the sandbox flag plumbed. AgentClient.buildMessages now merges
  > message.file_parts into the formatted content array directly; the
  > package and langchain pass array content through verbatim (verified).
  >
  > Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
- `2026-06-12` test: AIP-147 integration probe for sandbox file part emission
  > Runs the real AgentClient.buildMessages chain (real BaseClient, real
  > @librechat/agents formatters; only infra stubbed) and asserts the
  > sandbox file content part lands in the LLM payload — the end-to-end
  > coverage gap that let KI-1 ship.
  >
  > Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
- `2026-06-12` fix: AIP-147 persist metadata.sandboxFileId in the File schema
  > The metadata sub-schema whitelists keys, so Mongoose silently dropped
  > sandboxFileId on save (rows had only toolResource) — the actual reason
  > no file content part ever reached the wire, regardless of the client
  > fixes. Verified against live Mongo rows. Pre-fix uploads have no
  > sandboxFileId and need re-attaching.
  >
  > Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
- `2026-06-12` feat: AIP-147 route chat CSV/XLSX uploads to the sandbox Files API
  > https://linear.app/100xteam/issue/AIP-147/route-chat-csvxlsx-uploads-to-the-sandbox-files-api-harness-endpoint
  >
  > * feat: AIP-147 route chat CSV/XLSX uploads to the sandbox Files API
  >
  > On custom endpoints with sandbox: true + sandboxFileUploadUrl (librechat.yaml),
  > tabular chat attachments upload to the sandbox runtime Files API instead of the
  > Code Interpreter, and attach to each turn as OpenAI file content parts. The
  > sandbox server owns conflict handling; deleting the attachment removes the
  > sandbox copy. Flag off keeps Agent Hub and the Agents endpoint unchanged.
  >
  > Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
  >
  > …

## `100x-ai-plugins` — Plugin marketplace / SDD harness (5)

- `2026-06-08` feat(linear-toolkit): post standup to ENGSTD channel + Linear formatter
  > Fix the daily-standup publish path: post the draft as a comment on the
  > dated ENGSTD standup issue via save_comment, not save_status_update
  > (which has no teamId and only attaches to a project/initiative).
  >
  > - Add §5a/5b: resolve today's "Daily standup <date>" issue in team
  >   ENGSTD, then save_comment on it; ask before inventing a missing issue.
  > - Keep save_status_update as an explicit project/initiative fallback (§5c).
  > - Tighten §4 formatter to Linear markdown: literal newlines, @displayName
  >   mentions, real workflow-state names, auto-linking identifiers.
  > - Update frontmatter, prerequisite tool list, output prompt, and example.
  >
  > Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
- `2026-06-08` feat(linear-toolkit): post standup to ENGSTD channel + Linear formatter (#7)
  > Fix the daily-standup publish path: post the draft as a comment on the
  > dated ENGSTD standup issue via save_comment, not save_status_update
  > (which has no teamId and only attaches to a project/initiative).
  >
  > - Add §5a/5b: resolve today's "Daily standup <date>" issue in team
  >   ENGSTD, then save_comment on it; ask before inventing a missing issue.
  > - Keep save_status_update as an explicit project/initiative fallback (§5c).
  > - Tighten §4 formatter to Linear markdown: literal newlines, @displayName
  >   mentions, real workflow-state names, auto-linking identifiers.
  > - Update frontmatter, prerequisite tool list, output prompt, and example.
  >
  > Co-authored-by: Claude Opus 4.8 <noreply@anthropic.com>
- `2026-06-11` refactor(daily-standup): update bullet format for Linear integration
  > Revised the daily standup markdown to use bare issue identifiers for better integration with Linear. Adjusted entries for yesterday, today, and blockers to reflect current tasks and statuses without repeating titles or statuses. Ensured compliance with new formatting rules for clarity and consistency.
- `2026-06-11` Merge branch 'main' of github.com:100xteam-research/100x-ai-plugins into enhance/daily-standup-engstd-channel
- `2026-06-11` daily standup engstd channel (#8)
  > * feat(linear-toolkit): post standup to ENGSTD channel + Linear formatter
  >
  > Fix the daily-standup publish path: post the draft as a comment on the
  > dated ENGSTD standup issue via save_comment, not save_status_update
  > (which has no teamId and only attaches to a project/initiative).
  >
  > - Add §5a/5b: resolve today's "Daily standup <date>" issue in team
  >   ENGSTD, then save_comment on it; ask before inventing a missing issue.
  > - Keep save_status_update as an explicit project/initiative fallback (§5c).
  > - Tighten §4 formatter to Linear markdown: literal newlines, @displayName
  >   mentions, real workflow-state names, auto-linking identifiers.
  > - Update frontmatter, prerequisite tool list, output prompt, and example.
  > …

## `vireox-runtime-plugin` — Vireox runtime plugin (5)

- `2026-06-09` Scaffold runtime plugin root with vireox-all-agents team + tooling
  > Set up this repo as a 100x-agent-runtime plugin root: each folder under
  > plugins/ is one agent team mirrored from the Vireox Agent Hub.
  >
  > - plugins/vireox-all-agents/: pilot team from the "VireoX All Agents" router
  >   (12 member subagents, 8 skills with bundled references, 2 MCP servers).
  >   AGENTS.md + member bodies are verbatim Agent Hub system prompts; member
  >   description := role; mcp -> mcpServers; native/internal tools & MCP excluded;
  >   .mcp.json has type+url only (no secrets).
  > - .claude/skills/generate-team-from-agent-hub/: reusable skill + generalized
  >   generator to pull Agent Hub data via Chrome and emit a team by name/id.
  > - scripts/validate_plugins.py + .githooks/pre-commit + .pre-commit-config.yaml:
  >   pre-commit gate validating manifest, agents, skills, and MCP.
  > …
- `2026-06-09` Use public connector URLs for team MCP servers
  > Rewrite Agent Hub internal MCP urls to the public gateway
  > (https://connector.100xteam.ai/vireox/<name>/mcp) so teams are reachable
  > off-network. Applied to vireox-all-agents (.mcp.json) and the generator
  > (MCP_CONNECTOR_BASE / public_mcp_url, with MCP_URL_OVERRIDES for exceptions).
  >
  > Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
- `2026-06-09` Set team access gate to none; make member access explicit
  > - plugin.json: skills/mcpServers = [] (expose-none gate). The team accesses
  >   no skills/MCP directly; subagents declare their own and the runtime host
  >   provides them (vireox-askdata reference pattern). Bundled skills/ + .mcp.json
  >   are still shipped.
  > - agents/*.md: always emit skills: [] and mcpServers: [] so each member's
  >   access is explicit even when empty.
  > - generator + conventions.md updated to match.
  >
  > Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
- `2026-06-09` Adopt D-020 manifest layout + add docs, root SKILL.md, Claude-loadability check
  > - D-020: plugin.json is Claude-compatible — top-level skills/mcpServers/model
  >   removed; team model + coordinator access gate (D-018/D-019) move under the
  >   x-agent-hub vendor key. `claude plugin validate` now passes with a single
  >   (expected) x-agent-hub warning.
  > - Dual team prompt: each team gets a root SKILL.md (Claude `/<plugin>:<slug>`)
  >   that imports @AGENTS.md with disable-model-invocation: true; runtime keeps
  >   using AGENTS.md.
  > - docs/: claude-plugin-reference, compatibility (incl. a TODO note that member
  >   mcpServers is unsupported for plugin agents in Claude — use tools later),
  >   access-control (Claude vs runtime ACL).
  > - validate_plugins.py: reads the gate from x-agent-hub.access, warns on
  >   top-level skills/mcpServers/model, validates the root SKILL.md, and runs a
  > …
- `2026-06-09` docs: note D-021 (MCP tool names mcp__server__tool match in both systems)
  > Refine the compatibility follow-up: a `tools` entry written for Claude
  > (`mcp__<server>__<tool>`) is honored verbatim by the runtime matcher. Remaining
  > nuances captured (whole-server token, tools:[] semantics, replace-vs-carry-both).
  >
  > Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
