# Week of 20 April 2026

> **Range:** Mon 20 Apr – Sun 26 Apr 2026  
> **Activity:** 29 commits across 3 repos

| Repository | Commits | Domain |
|---|---|---|
| `100x-agent-hub` | 19 | Backend agent platform |
| `100x-agent-hub-ui` | 7 | Frontend admin UI |
| `100x-data-platform` | 3 | SharePoint→Kanban ETL |

---

## `100x-agent-hub` — Backend agent platform (19)

- `2026-04-20` feat: add filtering options to report listing API
  > - Introduced `search` parameter for case-insensitive substring search over `notifications.title` and `email_info.subject`, enhancing report retrieval capabilities.
  > - Added `recipient` parameter to filter reports by recipient email in `notifications.recipients`, allowing for more targeted queries.
  > - Implemented regex-based filtering logic to support the new search functionalities, improving user experience in navigating reports.
- `2026-04-20` feat: enhance Knowledge Base Platform documentation
  > - Added comprehensive instructions for the CO3 Pipeline Copilot, detailing knowledge retrieval processes and best practices for document filtering.
  > - Introduced a new glossary to define key terms used across the Knowledge Base Platform, improving clarity for users and developers.
  > - Created an MCP integration guide to assist engineers and AI agent builders in connecting with the Knowledge Base Platform, outlining endpoint usage and tool descriptions.
  > - Developed an overview document to summarize the platform's purpose, functionality, and user personas, facilitating better understanding for all stakeholders.
- `2026-04-20` feat: enhance test_send_email script with bulk email functionality
  > - Added support for bulk email sending through the `--bulk` argument, allowing users to send emails to multiple generated test addresses.
  > - Implemented a helper function `_generate_bulk_recipients` to create a list of email addresses based on a specified count, ensuring compliance with AWS SES's 50-destination limit.
  > - Updated argument parsing to require either a recipient email or the `--bulk` flag, improving usability and error handling.
  > - Enhanced error handling for bulk email sending with detailed output for failed batches and recipients.
- `2026-04-20` feat: implement SES email batching and notification cleanup
  > - Introduced `SESClient.send_email_bulk` to handle batching of email recipients, addressing AWS SES's 50-destination limit and improving email delivery reliability.
  > - Updated `UserReportGenerator.send_email_notification` to utilize the new bulk sending method, enhancing error handling for partial failures with `SESBulkSendError`.
  > - Cleaned up the notification process by removing stale retry logic and dead code, ensuring clarity in the email sending workflow.
  > - Revised documentation to reflect changes in email notification behavior and updated alert mechanisms for email send failures.
- `2026-04-20` feat: knowledge base progress
  > * Enhance knowledge base API documentation and retrieval services
  >
  > - Updated the `knowledge_base_mcp.md` documentation to include detailed descriptions of the `browse_documents` tool and filter value syntax, improving user understanding of the knowledge base retrieval process.
  > - Introduced a new `FilterTooBroadError` exception in the `knowledge_base_service.py` to handle cases where filters match too many documents, enforcing stricter retrieval limits.
  > - Refactored document retrieval methods to utilize a new utility function for building MongoDB queries from agent-facing filters, enhancing code clarity and maintainability.
  > - Added unit tests for the new query utility functions to ensure …
- `2026-04-21` feat: implement V2 email delivery and sampling enhancements
  > - Introduced per-recipient email delivery in `UserReportGenerator`, ensuring each recipient sees only their own address, improving privacy and error handling.
  > - Added support for multi-draft sampling in V2 notifications, controlled by `REPORT_V2_ENABLE_SAMPLING` and `REPORT_V2_SAMPLING_NUM_REPORTS` settings, enhancing report quality.
  > - Updated documentation to reflect changes in email delivery behavior and sampling configuration.
  > - Implemented unit tests to validate new features and ensure robust error handling for email sending.
- `2026-04-22` feat: browse knowledge base improvement
  > * init
  >
  > * feat: enhance browse_documents tool and documentation
  >
  > - Updated the `browse_documents` tool to support optional `filters` and a free-text `query`, improving document retrieval capabilities.
  > - Enhanced the overview response to include a token-budget-aware file-path tree, providing a clearer structure for users.
  > - Introduced a new `build_browse_overview` function to manage the rendering of document overviews, ensuring efficient token usage.
  > - Updated documentation to reflect changes in the `browse_documents` tool, including detailed instructions on using filters and queries for better user guidance.
  > - Added integration tests for the new wildcard `$text` index functionality, …
- `2026-04-22` feat: enhance browse_documents tool and documentation
  > - Updated the `browse_documents` tool to support optional `filters` and a free-text `query`, improving document retrieval capabilities.
  > - Enhanced the overview response to include a token-budget-aware file-path tree, providing a clearer structure for users.
  > - Introduced a new `build_browse_overview` function to manage the rendering of document overviews, ensuring efficient token usage.
  > - Updated documentation to reflect changes in the `browse_documents` tool, including detailed instructions on using filters and queries for better user guidance.
  > - Added integration tests for the new wildcard `$text` index functionality, ensuring robust search capabilities across document metadata.
- `2026-04-22` feat: enhance document rendering and counting logic in knowledge base
  > - Added `_effective_parent` and `_path_from` functions to manage folder relationships after pass-through collapse, ensuring descendants are correctly attached to their nearest kept ancestor.
  > - Implemented logic to prevent double counting of documents in rollup buckets when both a pruned folder and its descendant are present, improving accuracy in document counts.
  > - Updated the indented render function to include descendants of collapsed folders, maintaining visibility of the document structure.
  > - Added regression tests to validate the new rendering and counting behavior, ensuring robustness against previous bugs.
- `2026-04-22` feat: optimize folder rendering logic in browse_overview
  > - Introduced `_effective_children_map` function to compute folder relationships in a single pass, improving rendering efficiency from O(k² × d) to O(k × d).
  > - Updated `_render_indented` and `_render_branch` functions to utilize pre-computed children mapping, enhancing performance and maintaining accurate document visibility.
  > - Ensured robustness against pass-through collapse of intermediate folders, improving the accuracy of displayed document structures.
  > - Added comments to clarify the logic and purpose of new functions for better maintainability.
- `2026-04-23` feat: enhance CO3 Pipeline Copilot instructions and add queue & retry metrics documentation
  > - Updated the CO3 Pipeline Copilot instructions to include detailed stages of the Kanban pipeline, improving clarity on deal progression and document context.
  > - Added a new document on queue and retry metrics for the knowledge base, outlining metric definitions, aggregation pipeline, and recommended indexes to optimize performance.
  > - Included caveats and example outputs to guide users in understanding the metrics and their implications for document processing.
- `2026-04-24` feat(agent-mcp): unify agent MCP integration with mcpserverconfigs catalog
  > Refactors Agent Hub and LibreChat agent configuration to resolve tools
  > against the shared `mcpserverconfigs` collection, producing a single
  > `UnifiedAgent` shape across both sources.
  >
  > Highlights
  > - Introduce `MCPToolResolver` + `classify_leftover_tools` + `MCPServerBindingView`
  >   so raw `tools[]` strings map to explicit MCP server bindings; legacy
  >   `database_mcp`/`reasoning_mcp`/`code_execution_mcp`/`report_tools_mcp`
  >   booleans are kept as storage-only back-compat.
  > - Add `MCPServerConfigService` (5-min cached catalog + `mask_headers`) and
  >   `probe_mcp_server_config` for live tool/instruction discovery; expose
  >   `/api/v1/mcp-servers` and `/mcp-servers/{id}/details` endpoints.
  > - Refactor …
- `2026-04-25` feat(agent-hub): introduce AgentNotFoundError for improved error handling
  > - Added a new `AgentNotFoundError` exception to be raised when an agent ID cannot be resolved, enhancing clarity in error reporting.
  > - Updated the `build_agent_runtime` method to raise `AgentNotFoundError` instead of returning None, preventing potential AttributeErrors in external callers.
  > - Modified member agent loading to tolerate stale references, logging a warning when a member agent is not found while maintaining the integrity of the overall process.
  >
  > This change improves the robustness of the agent hub by ensuring that error handling is more explicit and informative.
- `2026-04-25` fix(agent-hub): improve deprecation warning handling for tools
  > - Emit deprecation warnings for tools upfront, ensuring that all deprecated tool usages are logged regardless of the branch taken in the tool resolution process.
  > - Distinguish between unknown tool IDs and known sentinel tools that require special handling, enhancing clarity in error reporting.
  > - Refactor tool loading logic to improve robustness and maintainability, ensuring that only callable tool functions are processed.
  >
  > This change enhances the agent hub's error handling and provides clearer feedback on deprecated tool usage.
- `2026-04-25` fix(agent-mcp): clear legacy MCP bools on update, cache team members, guard deprecation warning
  > - update_agent: when mcp_servers is written, reset the legacy database_mcp /
  >   reasoning_mcp / code_execution_mcp / report_tools_mcp flags so stale bools
  >   don't silently re-add servers the user removed via the new API.
  > - _build_members: route through build_agent_runtime instead of
  >   build_agent_runtime_from_unified so member builds share the cache; a member
  >   appearing in two teams within the TTL now hits cache instead of rebuilding.
  > - _get_tools: only call warn_deprecated_tool_use for ids in DEPRECATED_TOOL_IDS
  >   (no behavior change; makes intent at the call site explicit).
  >
  > Made-with: Cursor
- `2026-04-25` fix(agent-mcp): safeguard mcp_servers updates and refine server instructions handling
  > - Updated the `update_agent` function to check for explicit presence of `mcp_servers` in `agent_data.model_fields_set`, preventing unintended overwrites during partial updates.
  > - Enhanced the logic in `mcp_tools_from_mcp_config` to align with the documented contract for `server_instructions`, ensuring correct handling of `None` and `False` values.
  > - Added unit tests to validate the behavior of server instructions loading, confirming compliance with expected outcomes.
  >
  > Made-with: Cursor
- `2026-04-24` refactor: improve error handling in MCP servers listing and update agent runtime documentation
  > - Updated the error handling in the `list_mcp_servers` function to prevent leaking sensitive information by logging the full traceback while returning a generic error message to the client.
  > - Refactored the agent runtime documentation to clarify the changes in instruction rendering and tool assembly processes, ensuring consistency in the agent's prompt structure.
  > - Introduced new helper functions for better organization and clarity in the agent hub factory's internal build steps.
- `2026-04-22` docs: update MCP integration documentation and enhance browse_documents tool description
  > - Removed introductory note for new users to streamline the document.
  > - Enhanced the `browse_documents` tool description to clarify its functionality, including optional parameters for metadata filters and free-text queries.
  > - Updated the overview response details to reflect a token-budget-aware file-path tree structure, improving user comprehension.
  > - Clarified the response fields and added detailed instructions on interpreting the file-path tree and its implications for document retrieval.
  > - Added regression tests to ensure the fallback logic for small trees is based on kept-folder count, not total rendered lines, improving accuracy in document representation.
- `2026-04-21` init

## `100x-agent-hub-ui` — Frontend admin UI (7)

- `2026-04-20` feat(reports): add search and recipient filters to reports list UI
  > - Introduced two new filters, `search` and `recipient`, to enhance the reports list functionality, allowing users to search by notification titles or email subjects and filter by specific recipients.
  > - Updated the `ReportFilter` type to include the new fields and modified the `getReportsCursor` service to append these parameters to the request.
  > - Enhanced the reports page UI to accommodate the new filters, ensuring they integrate seamlessly with existing filter functionality and maintain cursor pagination.
  > - Adjusted the layout to support the additional filters while preserving the user experience.
- `2026-04-24` feat(agents): migrate to unified MCP server bindings and add catalog UI
  > Replace the four legacy *_mcp booleans (database/reasoning/code_execution/
  > report_tools) on the Agent contract with a single `mcpServers: MCPServerBinding[]`
  > field, per the Agents API v2 spec. Surface the new `unsupportedTools[]` read-only
  > field as a warning on ToolsTab and AgentCard. Normalize `execute_code ->
  > code_interpreter` on write via a shared `normalizeToolIds` helper.
  >
  > Add the supporting MCP server catalog feature that backs the new binding picker:
  > a `/mcp-servers` page with search/filter/status facets, a details modal with
  > live probe (initialize + list_tools), and a Zustand store + service hitting
  > `GET /api/v1/mcp-servers` and `/{id}/details`.
  >
  > ToolsTab edits are gated on …
- `2026-04-25` feat(agents): add functional save handler for agent details and tools tab
  > - Introduced a new `onSaveFn` prop in `AgentDetails` and `ToolsTab` components to allow functional updates to the agent's state, ensuring that rapid consecutive changes do not overwrite each other.
  > - Updated `handleSaveFromTabFn` in `AgentDetailsPage` to compute the latest state before applying changes, improving data integrity during updates.
  > - Enhanced `toggleMcpServer` and `removeStaleBinding` methods in `ToolsTab` to utilize the new functional save approach, preventing data loss during rapid interactions.
- `2026-04-20` fix(knowledge): enhance upload handling and improve drag-and-drop experience in KnowledgeBaseDocumentsTab
  > - Updated the onUpload prop to support both void and Promise<void> return types, allowing for asynchronous upload handling.
  > - Improved the clearAllFilters function to reset the debounced query, ensuring accurate filtering during the debounce period.
  > - Enhanced the handleDragLeave function to prevent flickering by maintaining the drag highlight when the pointer moves over child elements.
  > - Refined the handleSubmitUpload function to manage the upload state more effectively, ensuring the upload dialog closes only after the upload process is complete.
- `2026-04-24` refactor(agents): simplify tool handling in ToolsTab and update normalization logic
  > - Removed the `normalizeToolIds` utility from the ToolsTab component, directly using the agent's tools for default values and onValueChange.
  > - Updated comments in the agentStore to clarify the normalization process for tools, ensuring that UI layers do not re-normalize already normalized data.
  > - Cleaned up MCP server store by removing the unused `getServerById` method, streamlining the store's API.
- `2026-04-25` refactor(agents): enhance reasoning effort field handling in BasicTab
  > - Introduced utility functions to read number and string fields from the reasoning effort object, improving code clarity and maintainability.
  > - Updated logic for determining budget tokens and reasoning levels to accommodate the new reading methods, ensuring accurate rendering based on available data.
  > - Revised comments to reflect the changes in data handling and clarify the rationale behind the new approach.
- `2026-04-25` refactor(agents): improve state management in AgentDetailsPage and ToolsTab
  > - Introduced a `useRef` for the `agent` in `AgentDetailsPage` to ensure the latest state is referenced in callbacks, preventing stale closures during updates.
  > - Updated `handleSaveFromTabFn` to utilize the new `agentRef`, enhancing data integrity during rapid state changes.
  > - Modified `ToolsTab` to prevent duplicate MCP server bindings when adding new entries, improving user experience and data consistency.

## `100x-data-platform` — SharePoint→Kanban ETL (3)

- `2026-04-21` sharepoints: add document error detail and update widget configurations in kanban notebooks
- `2026-04-21` sharepoints: implement indexing attempt tracking and enhance kanban documentation
- `2026-04-22` sharepoints: enhance attachment status handling by adding file type validation and updating documentation
