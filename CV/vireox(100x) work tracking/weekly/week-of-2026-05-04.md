# Week of 4 May 2026

> **Range:** Mon 4 May – Sun 10 May 2026  
> **Activity:** 19 commits across 3 repos

| Repository | Commits | Domain |
|---|---|---|
| `100x-agent-hub` | 10 | Backend agent platform |
| `100x-agent-hub-ui` | 8 | Frontend admin UI |
| `100x-data-platform` | 1 | SharePoint→Kanban ETL |

---

## `100x-agent-hub` — Backend agent platform (10)

- `2026-05-04` feat(agent-mcp): unify agent MCP integration with mcpserverconfigs ca…
  > * feat(agent-mcp): unify agent MCP integration with mcpserverconfigs catalog
  >
  > Refactors Agent Hub and LibreChat agent configuration to resolve tools
  > against the shared `mcpserverconfigs` collection, producing a single
  > `UnifiedAgent` shape across both sources.
  >
  > Highlights
  > - Introduce `MCPToolResolver` + `classify_leftover_tools` + `MCPServerBindingView`
  >   so raw `tools[]` strings map to explicit MCP server bindings; legacy
  >   `database_mcp`/`reasoning_mcp`/`code_execution_mcp`/`report_tools_mcp`
  >   booleans are kept as storage-only back-compat.
  > - Add `MCPServerConfigService` (5-min cached catalog + `mask_headers`) and
  >   `probe_mcp_server_config` for live tool/instruction discovery; expose …
- `2026-05-05` feat(kb): add bulk delete endpoint for documents
  > - Introduced a new API endpoint `POST /knowledge-bases/{kb_id}/documents/bulk-delete` for deleting multiple documents in a single request, enhancing the document management capabilities.
  > - Implemented request and response schemas for the bulk delete operation, ensuring structured handling of document IDs and detailed feedback on deletion outcomes.
  > - Added validation to prevent deletion of documents currently in `indexing` or `processing` states, safeguarding against potential data integrity issues.
  > - Developed comprehensive integration tests to validate the functionality of the new endpoint, ensuring correct behavior under various scenarios, including handling of partial failures and input …
- `2026-05-05` feat(kb): add manual retry endpoints for documents in error status
  > - Introduced two new API endpoints: `POST /knowledge-bases/{kb_id}/documents/retry-failed` for bulk retrying documents in `error` status and `POST /knowledge-bases/{kb_id}/documents/{document_id}/retry` for retrying a single document.
  > - Implemented request and response schemas for the retry operations, ensuring structured handling of document IDs, data source IDs, and maximum document limits.
  > - Enhanced error handling to provide user-friendly messages for indexing failures, improving the overall user experience when dealing with document ingestion issues.
  > - Added integration tests to validate the functionality of the new endpoints, ensuring correct behavior under various scenarios.
  >
  > This …
- `2026-05-06` feat(kb): add manual retry endpoints for documents in error status
  > * feat(kb): add manual retry endpoints for documents in error status
  >
  > - Introduced two new API endpoints: `POST /knowledge-bases/{kb_id}/documents/retry-failed` for bulk retrying documents in `error` status and `POST /knowledge-bases/{kb_id}/documents/{document_id}/retry` for retrying a single document.
  > - Implemented request and response schemas for the retry operations, ensuring structured handling of document IDs, data source IDs, and maximum document limits.
  > - Enhanced error handling to provide user-friendly messages for indexing failures, improving the overall user experience when dealing with document ingestion issues.
  > - Added integration tests to validate the functionality of the new …
- `2026-05-05` fix(kb): correct skipped_count logic in requeue_failed_documents
  > - Updated the `requeue_failed_documents` method to ensure that `skipped_count` accurately reflects only genuinely un-retriable document IDs when the number of requested IDs exceeds the effective cap.
  > - Introduced a new calculation for `deferred_count` to track retriable documents that are not queued due to cap limitations, enhancing clarity in the logging output.
  > - Added unit tests to validate the new behavior, ensuring that skipped counts are not inflated by cap truncation and that deferred documents are correctly identified for follow-up requests.
  >
  > This change improves the accuracy of document requeuing metrics, providing better insights into the processing of failed documents.
- `2026-05-05` fix(mcp): resolve case-insensitive header collisions in MCP configuration
  > Updated the header merging logic in the MCP configuration to handle case-insensitive collisions correctly. The config-authored key is preserved while the runtime overlay's value takes precedence. Added tests to ensure that headers like `X-Conversation-ID` and `authorization` maintain their original casing in the config while resolving to the correct runtime values. This change enhances the integrity of header management in MCP interactions.
- `2026-05-07` fix(providers): coerce page_number to int for cleaner metadata handling
  > Updated the Bedrock and BedrockS3 knowledge base implementations to ensure that page numbers, which are returned as floats, are coerced to integers. This change improves the consistency and cleanliness of the metadata used downstream, enhancing overall data integrity.
- `2026-05-07` fix(providers): comment out location field in Bedrock knowledge base
  > Removed the `location` field from the metadata in both Bedrock and BedrockS3 knowledge base implementations to prevent potential issues with undefined or unnecessary data. This change enhances the stability of the knowledge base structure while maintaining existing functionality.
- `2026-05-06` refactor(api): streamline datetime handling in LibreChat and MCP server responses
  > - Removed the `_ensure_utc_iso` utility function from `librechat.py` and `mcp_servers.py`, replacing its usage with direct calls to `isoformat()` on datetime objects. This simplifies the code and improves readability.
  > - Updated the `createdAt` and `updatedAt` fields in response models to use `isoformat()` directly, ensuring consistent datetime formatting across the API.
  > - Enhanced the `agent_config_service.py` to set `created_at` and `updated_at` to the current time in UTC, improving the accuracy of timestamps.
  >
  > These changes enhance the clarity and maintainability of the codebase by reducing redundancy in datetime handling.
- `2026-05-07` refactor(providers): namespace KB chunk metadata under document.*
  > Reshape Bedrock retrieval chunks into a stable, predictable contract
  > so callers can rely on a fixed key set instead of raw Bedrock-internal
  > keys (`fileId`, `filename`, `x-amz-bedrock-kb-*`) that may change
  > underneath us.
  >
  > - Curate chunk metadata into `document.id`, `document.name`,
  >   `document.page_number`, `document.content_source`; drop the inline
  >   `(Source: ...)` / `(Audio source: ...)` lines from `content` so the
  >   body stays clean for the LLM and citations are rendered from
  >   metadata.
  > - Forward any user-defined metadata that came in via the document's
  >   `.metadata.json` sidecar so the Agno retriever path (which bypasses
  >   `enrich_chunks_with_document_metadata`) does not lose custom …

## `100x-agent-hub-ui` — Frontend admin UI (8)

- `2026-05-04` feat(agents): migrate to unified MCP server bindings and add catalog UI
  > * feat(agents): migrate to unified MCP server bindings and add catalog UI
  >
  > Replace the four legacy *_mcp booleans (database/reasoning/code_execution/
  > report_tools) on the Agent contract with a single `mcpServers: MCPServerBinding[]`
  > field, per the Agents API v2 spec. Surface the new `unsupportedTools[]` read-only
  > field as a warning on ToolsTab and AgentCard. Normalize `execute_code ->
  > code_interpreter` on write via a shared `normalizeToolIds` helper.
  >
  > Add the supporting MCP server catalog feature that backs the new binding picker:
  > a `/mcp-servers` page with search/filter/status facets, a details modal with
  > live probe (initialize + list_tools), and a Zustand store + service hitting
  > `GET …
- `2026-05-05` feat(bulk-delete): implement bulk delete functionality for documents in Knowledge Base
  > - Added a new UI for bulk deleting documents, allowing operators to select multiple documents and delete them in a single action.
  > - Introduced a confirmation modal for bulk delete actions, providing feedback on the success or failure of each document deletion.
  > - Updated the API service to handle bulk delete requests, processing up to 200 document IDs per call and returning detailed success/failure reports.
  > - Enhanced the KnowledgeBaseDocumentsTab component to support document selection and integrate the new bulk delete functionality.
  > - Implemented state management for selected documents and ensured UI consistency during the deletion process, including handling partial failures gracefully.
  > - …
- `2026-05-05` feat(kb-error-remediation): implement UI for error handling and document retry actions
  > - Introduced a new Knowledge Base Error Remediation UI document detailing the implementation of inline error messages and retry actions for errored documents.
  > - Added per-row "Retry" buttons for individual document recovery and a bulk "Re-index errored documents" action, enhancing user experience and operational efficiency.
  > - Updated API service to support new retry endpoints and modified the store to handle optimistic updates for document statuses.
  > - Enhanced the KnowledgeBaseDocumentsTab component to display error messages and integrate new retry functionalities, ensuring a seamless user interaction with errored documents.
  > - Updated types to accommodate new document statuses and error …
- `2026-05-06` feat(kb-error-remediation): implement UI for error handling and docum…
  > * feat(kb-error-remediation): implement UI for error handling and document retry actions
  >
  > - Introduced a new Knowledge Base Error Remediation UI document detailing the implementation of inline error messages and retry actions for errored documents.
  > - Added per-row "Retry" buttons for individual document recovery and a bulk "Re-index errored documents" action, enhancing user experience and operational efficiency.
  > - Updated API service to support new retry endpoints and modified the store to handle optimistic updates for document statuses.
  > - Enhanced the KnowledgeBaseDocumentsTab component to display error messages and integrate new retry functionalities, ensuring a seamless user interaction …
- `2026-05-06` feat(reports): enhance report workflows with status filtering and date formatting
  > - Added a new status filter to the report workflows page, allowing users to filter by workflow status (pending, completed, failed).
  > - Implemented a function to format report dates based on the user's timezone, improving date display consistency.
  > - Updated the report detail page to show the frequency of notifications and the report date formatted in the user's timezone.
  > - Enhanced the UI to accommodate the new status column in the workflows table, ensuring a better user experience when viewing report workflows.
- `2026-05-07` feat(reports): add EmailBodyPreview component for secure email rendering
  > - Introduced a new EmailBodyPreview component to render email HTML securely within a shadow DOM, ensuring CSS isolation and preventing script execution.
  > - Integrated the EmailBodyPreview into the report detail page, replacing the previous method of rendering email body content with improved security and styling.
  > - Implemented DOMPurify for sanitizing email content, enhancing security against XSS attacks while maintaining a clean and user-friendly display.
- `2026-05-05` refactor(kb-error-remediation): enhance document retry logic and UI consistency
  > - Updated the `retryFailedDocuments` logic to ensure that when `documentIds` are provided, all targeted documents are optimistically updated immediately, with proper restoration of un-queued documents based on server response.
  > - Clarified the handling of document IDs in the `KnowledgeBaseDocumentsTab` to ensure the visible errored documents are always passed explicitly, improving consistency between user interface counts and queued documents.
  > - Enhanced the optimistic update mechanism in the store to maintain UI consistency with server state, addressing potential discrepancies during concurrent updates.
  > - Improved documentation and comments for better understanding of the retry process and …
- `2026-05-07` chore(deps): update axios and follow-redirects versions in package.json and package-lock.json
  > - Updated axios from version 1.15.0 to 1.15.2 for improved performance and security.
  > - Updated follow-redirects from version 1.15.11 to 1.16.0 to address potential vulnerabilities.

## `100x-data-platform` — SharePoint→Kanban ETL (1)

- `2026-05-05` sharepoints: implement KnowledgeBaseAPIError handling and enhance file upload validation in kanban notebooks
