# Week of 18 May 2026

> **Range:** Mon 18 May – Sun 24 May 2026  
> **Activity:** 21 commits across 5 repos · 15 Linear tickets

| Repository | Commits | Domain |
|---|---|---|
| `100x-agent-hub` | 7 | Backend agent platform |
| `100x-ai-plugins` | 6 | Plugin marketplace / SDD harness |
| `100x-mcp-gateway` | 5 | MCP gateway |
| `100x-agent-hub-ui` | 2 | Frontend admin UI |
| `agno` | 1 | Agno framework (upstream OSS) |

---

## 🎫 Linear tickets

**Completed (4)**

- ✅ **AIP-23** (completed) — Agent Hub: Add support for GPT 5.5  
  _Done · High priority_
- ✅ **AIP-26** (completed) — Investigate agent hub API high/leak CPU  
  _Done · Medium priority_
- ✅ **INF-3** (completed) — [AgentHub] CVE-2026-42284 — GitPython  
  _Done · High priority_
- ✅ **INF-4** (completed) — [AgentHub] Health check  
  _Done · Low priority_

**Opened / in progress (11)**

- 🚫 **AIP-24** (opened) — Investigate Claude routine - adaptable with report workflow  
  _Canceled · High priority_
- 🚫 **AIP-25** (opened) — Sync agent skills to Claude skills platform  
  _Canceled · Low priority_
- ✅ **AIP-35** (opened) — Upstream agno: contribute lifecycle lock fix for MCPTools parallel connect race  
  _Done · Low priority, `Bug`_
- ✅ **AIP-36** (opened) — [Bug] Fix "document too large" error when saving workflow sessions to database  
  _Done · High priority, `v2.16.0`, `Bug`_
- ✅ **AIP-39** (opened) — [Bug] Notification Session ID link is invalid on Report Information page  
  _Done · Medium priority, `v2.16.0`, `Bug`_
- ✅ **AIP-46** (opened) — Enable pre-indexed KB testing in native Claude  
  _Done · High priority_
- ✅ **AIP-55** (opened) — Wire pre-indexed KB MCP into the gateway tenant via mcp_servers object form (X-Knowledge-Base-Id header)  
  _Done · Medium priority_
- ✅ **AIP-56** (opened) — [Bug] Rocky Mountain Report Failed  
  _Done · Urgent priority, `Bug`_
- ✅ **AIP-57** (opened) — fix(agents): forward explicit httpx.Timeout to Bedrock Claude SDK  
  _Done · Urgent priority, `Bug`_
- ✅ **AIP-68** (opened) — Port CACO + CO3 Pipeline Copilots to Claude plugin marketplace  
  _Done · Medium priority_
- 🚫 **AIP-69** (opened) — 100x-mcp-gateway: enable DCR or CIMD on the cag tenant so MCP plugins drop env.OAUTH_CLIENT_ID  
  _Canceled · Low priority_

---

## `100x-agent-hub` — Backend agent platform (7)

- `2026-05-18` feat(agents): add GPT-5.5 and GPT-5.5-pro to OpenAI model routing
  > Route gpt-5.5 / gpt-5.5-pro (and their dated snapshots) through the same
  > flagship-only logic that already covers gpt-5, gpt-5.2, gpt-5.4: omit
  > temperature so OpenAI provider defaults apply, and forward reasoning_effort
  > when the agent's ReasoningConfig.level is set.
  >
  > Without this, vireox_agents.agent.openai_model would silently send a
  > user-supplied temperature to a flagship reasoning model that ignores it,
  > and would never set reasoning_effort on the Responses API.
  >
  > Adds focused unit tests (40 cases) for the id-routing helpers and both
  > Chat / Responses builders, plus a feature doc tracking AIP-23.
  >
  > Refs: AIP-23
  > Unblocks: AIP-1 (background-mode report generation testing)
- `2026-05-18` fix(agents): re-raise MCPTools init failures in __aenter__
  > Follow-up to edd97ff9. Routing __aenter__ through the public connect()
  > silently swallowed init errors (connect()'s try/except is intentional for
  > the pre_run_hook iterate-every-tool path), leaving `async with MCPTools(...)
  > as tool:` callers with an uninitialized instance and a confusing NoneType
  > error on the next call instead of failing at the async-with boundary.
  >
  > __aenter__ now acquires _lifecycle_lock itself and calls _connect()
  > directly, preserving the single-flight concurrency safety while restoring
  > the raise-on-failure contract. The connect() / __aenter__ failure-contract
  > asymmetry is documented in the module README, design doc, and two
  > regression tests (one per side of the …
- `2026-05-18` fix(agents): serialize MCPTools connect/close to prevent BrokenResourceError
  > When a Team delegates multiple tool calls to the same member in parallel,
  > each delegation's arun() races on the shared MCPTools.connect(). All
  > callers pass the `not tool.initialized` check, each opens a fresh
  > streamablehttp_client session, and the losers get GC'd mid-flight —
  > their backing tasks tear down the anyio streams the surviving call is
  > still using, surfacing as `anyio.BrokenResourceError` from
  > `await session.list_tools()`.
  >
  > Add a lazy `_lifecycle_lock` (asyncio.Lock) shared between connect() and
  > close(), with a double-checked early return so the steady-state
  > "already connected" hot path stays lock-free. Split close() into a
  > public wrapper + `_close_locked()` body so …
- `2026-05-19` fix(api): repair MongoDB health check import path
  > The /health and /monitoring/health endpoints imported check_mongo_health
  > from the non-existent vireox.db.mongo module, causing every MongoDB
  > health check in production to fail with ModuleNotFoundError. Switch to
  > the real location (vireox_common_libs.db.mongo) and lift it to a
  > module-level import to match the postgres pattern.
  >
  > Fixes INF-4.
- `2026-05-20` fix(agents): bound Vertex Gemini requests with shared MODEL_READ_TIMEOUT_SECONDS
  > agno's Gemini model defaults to timeout=None, which propagates as
  > HttpOptions.timeout=None into google-genai and disables timeouts on the
  > underlying httpx/aiohttp client entirely. Vertex Gemini requests had no
  > safety net against an unbounded hang.
  >
  > Forward settings.MODEL_READ_TIMEOUT_SECONDS as a scalar via
  > Gemini(timeout=...). This gives every Vertex Gemini call the same total
  > budget as Anthropic/OpenAI clients. Note: google-genai's HTTP layer
  > accepts a single int of milliseconds, not an httpx.Timeout, so the
  > per-bucket connect/read/write/pool split used on Anthropic/OpenAI cannot
  > be carried over here — that's an SDK constraint.
  >
  > Two regression tests pin the wiring + the env-driven knob. …
- `2026-05-20` fix(agents): forward explicit httpx.Timeout to Bedrock Claude SDK
  > * fix(agents): forward explicit httpx.Timeout to Bedrock Claude SDK
  >
  > Bedrock Claude requests were timing out at 60s of inter-chunk silence
  > instead of the intended 1500s budget. agno's shared httpx.AsyncClient
  > (timeout=60s) was being injected into AsyncAnthropicBedrock, and the
  > Anthropic SDK adopts that client's timeout as the per-request timeout
  > whenever the caller doesn't pass timeout= explicitly. On extended-thinking
  > runs (e.g. the Rocky Mountain report workflow) inter-chunk gaps in the SSE
  > stream regularly exceed 60s, surfacing as httpcore.ReadTimeout. The agno
  > v2.5.15 upgrade fixed this for OpenAI internally but did not cover
  > Anthropic.
  >
  > This commit unifies the per-request timeout …
- `2026-05-19` chore(infra): patch CVE-2026-42284 (gitpython) and drop unused dep from agent-skills
  > GitPython <3.1.47 has a critical (CVSS 9.0) argument-injection issue in
  > _clone() / Repo.clone_from() / Remote.{fetch,pull,push} / Submodule.update
  > where multi_options is validated before shlex.split, so a string like
  > "--branch main --config core.hooksPath=/x" passes validation and runs
  > attacker-controlled hooks during clone (CVE-2026-42284, GHSA-rpm5-65cw-6hj4).
  >
  > The onehundredx-agent-skills package declared GitPython>=3.1.40 but never
  > imported it (it only wraps agno.skills.LocalSkills for on-disk loading), so
  > the direct dep is removed. GitPython is still pulled in transitively by
  > agno==2.5.16, so a workspace-level constraint (gitpython>=3.1.47) is added
  > to pyproject.toml; uv resolved it to …

## `100x-ai-plugins` — Plugin marketplace / SDD harness (6)

- `2026-05-21` feat: ship CACO + CO3 Pipeline Copilots and the chicago-atlantic-toolkit
  > Add the first agent-plugin pair backed by a shared vertical toolkit, plus
  > the SDD spine and an end-to-end OAuth verifier.
  >
  > Plugin layout follows the canonical-skill rule from the constitution:
  > chicago-atlantic-toolkit owns the four cross-deal skills (excel discovery
  > query, source-tier prioritization, deal-analyst rules, board write
  > protection); both copilots bundle byte-identical copies via the
  > sync-agent-skills helper. The two copilots only diverge in the agent
  > prompt, slash commands, and the .mcp.json knowledge-base server they
  > attach to.
  >
  > Plugins:
  > - vertical-plugins/chicago-atlantic-toolkit/ — skill source of truth
  > - agent-plugins/caco-pipeline-copilot/ — caco-knowledge-base + …
- `2026-05-21` fix: use full ./plugins/... source paths in marketplace.json
  > Per the Claude marketplace spec, metadata.pluginRoot is only prepended
  > to bare-name sources (e.g. "formatter"), and the JSON Schema requires
  > the "./" prefix on string sources. The previous combination —
  > pluginRoot: "./plugins" + source: "./agent-plugins/..." — silently
  > ignored pluginRoot and resolved sources directly against the marketplace
  > root, breaking `claude plugin install` with "Source path does not exist".
  >
  > - Drop metadata.pluginRoot; use full "./plugins/<category-dir>/<name>"
  >   source paths (matching the anthropics/financial-services reference).
  > - Fix scripts/check.py to match Claude's real resolution behavior so
  >   drift is caught locally instead of at install time.
  > - Update …
- `2026-05-21` chore: amend invariant 5 — allow non-secret OAuth env on HTTP MCP servers
  > The CAG connector at https://connector.100xteam.ai/cag has Dynamic Client
  > Registration disabled and does not advertise CIMD support, so an MCP client
  > walking the MCP authorization spec (RFC 9728 → RFC 8414 → auth_code+PKCE)
  > needs the pre-registered Cognito app client_id surfaced through .mcp.json
  > to start the /authorize step.
  >
  > Rather than carry a static bearer header (which loses the user-consent
  > property) or wait for Anthropic-held creds, this commit narrows the
  > "HTTP-only MCP" rule so it keeps blocking the stdio shape while permitting
  > non-secret OAuth client metadata in env. Secret-named keys are still
  > rejected by check.py; OAUTH_CLIENT_SECRET / bearer tokens / API keys
  > remain …
- `2026-05-21` chore: initial commit — Phase 0 harness + marketplace spine
  > Bootstrap a Claude plugin marketplace bundling reusable agents, skills, slash
  > commands, hooks, and MCP servers for Claude Code and Claude Cowork.
  >
  > Structure follows anthropics/financial-services (file-based, no build step,
  > Python validator) with the Spec-Driven Development + multi-agent harness
  > layered on top from 100xteam-ai/100x-mcp-gateway.
  >
  > Includes:
  > - Marketplace spine + sample vertical plugin (starter)
  > - Constitution with 7 non-negotiables and 3 trust-boundary artifacts
  > - Cursor and Claude Code harnesses (rules, commands, hooks, subagent)
  > - SDD scaffolding (specs/_template, /specify → /implement slash commands)
  > - Python validator (scripts/check.py), pre-merge gate …
- `2026-05-21` chore: relocate starter plugin → templates/starter-plugin/
  > Now that real plugins ship in this marketplace (chicago-atlantic-toolkit
  > + the two pipeline copilots), the placeholder "starter" entry no longer
  > earns its slot in the install picker — but the directory itself is still
  > needed because scripts/new-plugin.py copies it when scaffolding.
  >
  > Move the directory out of plugins/ rather than leave it parked there:
  >
  > - plugins/ now contains only real, installable plugins (no marketplace
  >   warning, no phantom "starter" entry, marketplace category enum drops
  >   the one-off "starter" slot).
  > - templates/starter-plugin/ is honest about what it is — a copy-source.
  > - check.py's marketplace-↔-plugins/ bidirectional rule still passes
  >   cleanly because the template …
- `2026-05-21` chore: rename marketplace categories — vertical→toolkit, agent→finance
  > Make the user-facing category labels in the install picker more business-
  > oriented. The directory layout (plugins/vertical-plugins/, plugins/agent-
  > plugins/) is intentionally NOT renamed — that's a structural concern
  > separate from the marketplace's user-facing taxonomy, and renaming it
  > would churn ~25 files for cosmetic gain.
  >
  > Decision: marketplace `category` is now a freeform user-facing label,
  > decoupled from the on-disk directory:
  >
  >   category   directory under plugins/   meaning
  >   -------    ------------------------   --------
  >   toolkit    vertical-plugins/          skill+command+MCP bundle (canonical
  >                                         skill source)
  >   finance    agent-plugins/ …

## `100x-mcp-gateway` — MCP gateway (5)

- `2026-05-20` feat(config): align mcp_servers schema with Claude/MCP standard (ADR-006)
  > Shipped via full SDD lifecycle in specs/001-mcp-server-config-schema/
  > (spec.md, clarifications.md, plan.md, tasks.md, validation.md — all gates
  > explicitly waived by user with "go ahead, do to end, not need confirm").
  > Promotes the docs-first draft at docs/mcp-server-config-schema.md from
  > "Draft — awaiting approval" to "Accepted (2026-05-20)".
  >
  > ## What
  >
  > Each entry under tenants[<n>].mcp_servers now accepts either:
  >
  >   - the existing URL-string shorthand (backward-compatible, normalized at load), OR
  >   - the Claude / MCP standard object form: { type?, url, headers? }
  >
  > The object form lets operators inject per-upstream service-to-service headers
  > (typical case: X-Upstream-API-Key) without running …
- `2026-05-20` feat(config): align mcp_servers schema with Claude/MCP standard + bootstrap agent harness & SDD workflow
  > * feat(harness): bootstrap multi-agent harness + spec-driven development workflow
  >
  > Lifts the project from informal docs-first practice to a structured,
  > spec-anchored development workflow with first-class AI-agent support.
  >
  > ## Multi-agent harness (ADR-004)
  >
  > - AGENTS.md as the universal agent guide; CLAUDE.md imports it
  > - .claude/{agents,commands,settings.json} — Claude Code integration with
  >   pre-allowed shell commands and the security-reviewer subagent
  > - .cursor/{commands,hooks,rules} — Cursor integration: 5 scoped rules
  >   (000-project-context, 010-bun-typescript, 020-tests, 030-security-sensitive,
  >   040-spec-driven-development), 2 hooks (biome-fix, warn-destructive)
  > - .mcp.json — …
- `2026-05-20` feat(harness): bootstrap multi-agent harness + spec-driven development workflow
  > Lifts the project from informal docs-first practice to a structured,
  > spec-anchored development workflow with first-class AI-agent support.
  >
  > ## Multi-agent harness (ADR-004)
  >
  > - AGENTS.md as the universal agent guide; CLAUDE.md imports it
  > - .claude/{agents,commands,settings.json} — Claude Code integration with
  >   pre-allowed shell commands and the security-reviewer subagent
  > - .cursor/{commands,hooks,rules} — Cursor integration: 5 scoped rules
  >   (000-project-context, 010-bun-typescript, 020-tests, 030-security-sensitive,
  >   040-spec-driven-development), 2 hooks (biome-fix, warn-destructive)
  > - .mcp.json — project-level MCP servers (context7 for live docs)
  > - .devcontainer/devcontainer.json — …
- `2026-05-20` fix(review): address Cursor Bugbot findings on PR #6
  > Three issues flagged by Cursor Bugbot at commit 47c5540. All real,
  > verified empirically (see notes per-finding). Fix scope:
  >
  > ## Medium — src/config.ts: prototype-pollution defense on server-name accumulator
  >
  > Bugbot #2f55c75d. Empirically verified: `JSON.parse` preserves `__proto__`
  > as an own property of the parsed object, but copying that key into a plain
  > `{}` accumulator with `acc[k] = v` silently moves the value to the
  > prototype slot (string assignment is a no-op; object assignment changes
  > the prototype). Net effect:
  >
  >   - The malicious server is invisible to Object.keys(tenant.mcp_servers)
  >   - But tenant.mcp_servers.url / .type / .headers all resolve via the
  >     prototype chain to …
- `2026-05-20` refactor(tests): rename KB_HEADER to KB_ID and add RFC 9728 discovery test
  > - Renamed constant `KB_HEADER` to `KB_ID` for clarity.
  > - Introduced a new test to validate that the `/kb/mcp` endpoint correctly resolves to the tenant authorization server, ensuring compliance with RFC 9728.
  > - Updated assertions in existing tests to reflect the constant name change.

## `100x-agent-hub-ui` — Frontend admin UI (2)

- `2026-05-18` feat(agents): add GPT-5.5 and GPT-5.5 Pro to supported model list
  > Unblocks testing of the background-mode report generation reliability
  > work (AIP-1) which requires GPT-5.5. The existing getModelProvider()
  > already routes gpt-5* through the OpenAI reasoning path, so no other
  > wiring changes are needed.
  >
  > Closes AIP-23.
- `2026-05-18` Merge pull request #123 from 100xteam-ai/thuan/aip-23-agent-hub-add-support-for-gpt-55
  > feat(agents): add GPT-5.5 and GPT-5.5 Pro to supported model list

## `agno` — Agno framework (upstream OSS) (1)

- `2026-05-20` fix: serialize MCPTools connect/close to prevent BrokenResourceError
  > When multiple coroutines call ``MCPTools.connect()`` in parallel on a
  > shared instance (e.g. a Team LLM emitting multiple delegate_task_to_member
  > calls targeting the same member agent in one assistant message, dispatched
  > via ``asyncio.gather``), every caller passes the
  > ``if self._initialized: return`` early-return at the same time, each opens
  > its own ``streamablehttp_client`` / ``sse_client`` session, and only the
  > last winner is kept on ``self.session``. The losers' anyio task-groups
  > get GC'd mid-run and ``await session.list_tools()`` on the survivor
  > raises ``anyio.BrokenResourceError``.
  >
  > Add a lazy ``_lifecycle_lock`` (``asyncio.Lock``) shared between
  > ``connect()``, ``close()``, …
