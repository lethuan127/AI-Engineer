# Week of 29 June 2026

> **Range:** Mon 29 Jun – Sun 5 Jul 2026 (through 1 Jul)  
> **Activity:** 60 commits across 4 repos · 16 Linear tickets

| Repository | Commits | Domain |
|---|---|---|
| `100x-agent-runtime` | 32 | Deep Agents runtime |
| `100x-web-application` | 23 | Web app API |
| `100x-managed-plugins` | 3 | Managed customer plugins |
| `vireox-runtime-plugin` | 2 | Vireox runtime plugin |

---

## 🎫 Linear tickets

**Completed (4)**

- ✅ **AIP-187** — Native (bubblewrap) sandbox backend — `RUNTIME_SANDBOX_BACKEND=native` _(Medium)_
- ✅ **AIP-185** — local-shell sandbox: unify file-tool + shell workspace namespace, install Python deps, sync plugin updates _(High)_
- ✅ **AIP-169** — Organization & personal knowledge connectors (SharePoint, Drive, S3, git) — Research _(Medium)_
- ✅ **PENG-317** — Plugin maintenance pattern: shared-org-memory feedback + scheduled re-verification _(Medium)_

**In progress / opened (6)**

- 🔵 **AIP-190** — Plugin orchestrator design proposal _(In Review, Medium)_
- 🔵 **AIP-189** — Improve file presentation: viewable links + LibreChat artifact rendering _(Ready for Test, High)_
- 🔵 **PENG-318** — Prod env agent couldn't generate hotlink _(Ready for Test, Medium)_
- ⚪ **PENG-311** — Observability + Evaluator should create views _(Todo, High)_
- 🔵 **PENG-309** — user group dashboard observability _(Triage)_
- 📋 **CENG-597** — Apply flat one-fact-per-file memory design to manage-memory SKILL.md _(Backlog, `Improvement`)_

**Canceled — superseded by AIP-190 (6)**

- ❌ **AIP-191** — [Design] Group & access model — Users ↔ Groups ↔ Plugins ↔ Skills
- ❌ **AIP-192** — [Spike] Service boundary — new backend vs. evolve scripts + CI
- ❌ **AIP-193** — Share-by-reference skill resolution (one master copy → many plugins)
- ❌ **AIP-194** — Propagate + publish pipeline (resolve refs → rebuild → publish to groups)
- ❌ **AIP-195** — Run-time session composition (union + dedup a user's plugins at session start)
- ❌ **AIP-196** — No-UI refinement loop (refine skill in Claude → push to git → auto-resync)

---

## `100x-agent-runtime` — Deep Agents runtime (32)

- `2026-06-29` AIP-189: viewable sandbox file links + agent-driven presentation (D-038, Phase 1)
  > Replace D-026's auto-appended public-link block with agent-driven file
  > presentation. Add authenticated GET /v1/files/workspace/{path} serving
  > inline-or-download by content type (HTML/SVG sandboxed, all inline nosniff);
  > remove the anonymous /public/{token} route and links.py. Rewrite the workspace
  > prompt block to surface files as sandbox:/ links + HTML artifact references.
  >
  > Phase 1 runtime only; Phase 1-web (sandbox:/ proxy + artifact render) and
  > Phase 2 (S3 image hosting + restricted shares) deferred.
- `2026-06-29` AIP-185 + AIP-187: local-shell workspace unification + unix_local sandbox backend (#38)
  > * AIP-185: unify local-shell workspace, bake sandbox deps, per-turn plugin re-seed
  >
  > Fixes three local-shell production issues (spec 011):
  > - execute shell now shares one workspace with the file tools via a two-address
  >   contract (file: /{session_id}/, shell: $CLAUDE_PROJECT_DIR/{session_id}/,
  >   plugins: $CLAUDE_PLUGIN_ROOT/<plugin>/), surfaced in _workspace_note
  > - runtime image bakes the bundled skills' deps (requirements-sandbox.txt +
  >   Node/pptxgenjs), mirroring docker/daytona/sandbox/Dockerfile
  > - LocalShellSandboxProvider.get re-seeds plugins every turn, content-hash gated,
  >   so source edits land without a restart
  >
  > Relates to PENG-318. Decision recorded as D-036.
  > …
- `2026-06-29` AIP-187: address Bugbot — propagate NODE_PATH; lock cross-process re-seed
  > - provider.py: forward NODE_PATH into the unix_local sandbox env (it runs
  >   --clearenv); without it `require('pptxgenjs')` (minimax-pptx) fails since the
  >   global npm prefix is found via NODE_PATH. The modules live under the already
  >   RO-bound /usr, so only the var needs forwarding.
  > - bootstrap.py: wrap the per-turn re-seed (rmtree/copytree + marker write) in a
  >   cross-process fcntl flock on <workspace_root>/.plugin-seed.lock, so multiple
  >   workers/pods sharing the workspace volume can't interleave into a partial
  >   plugin tree mid-turn.
- `2026-06-29` AIP-187: drop unused _FakeSandbox.instances (RUF012) in unix_local tests
- `2026-06-29` AIP-187: fix unix_local data-loss on destroy + provider lifecycle (review #36)
  > - workspace.py: destroy() no longer rmtree's a caller-provided persistent root
  >   (the per-user workspace with session files). Only a self-created ephemeral
  >   tmpdir is removed (_owns_root). Fixes data loss on process exit / aclose via
  >   the atexit/__aexit__ path.
  > - selector: unix_local branch now `try/finally: await provider.aclose()` so the
  >   provider is released deterministically at shutdown (safe now that destroy is
  >   guarded).
  > - tests: regression coverage for destroy preserving caller root / removing owned.
- `2026-06-29` Merge remote-tracking branch 'https-main' into 012-native-sandbox-backend
  > # Conflicts:
  > #	runtime/harness/sandbox/provider.py
- `2026-06-29` AIP-187: unix_local sandbox backend (bubblewrap + cgroups v2) (#37)
  > * AIP-187: unix_local sandbox backend (bubblewrap + cgroups v2)
  >
  > Adds a third sandbox backend, RUNTIME_SANDBOX_BACKEND=unix_local, that runs the
  > agent's execute shell + Deep Agents file tools inside a bubblewrap mount
  > namespace on the runtime host (spec 012). Strategic replacement for the AIP-185
  > two-address local-shell workaround; Daytona stays the kernel-isolated tier.
  >
  > - Vendored runtime/harness/sandbox/unix_local/ from john221wick/deepagents-sandbox
  >   (MIT, pinned SHA; see NOTICE), patched with SandboxConfig.workspace_root so
  >   /workspace binds a stable per-user host dir. Name follows the OpenAI Agents SDK
  >   unix_local convention (ours uses bubblewrap; theirs adds no Linux confinement).
  > - UnixLocalSandboxProvider: per-user /workspace, read-only live plugin mounts at
  > …
- `2026-06-29` AIP-187: record local Docker verification (unix_local) in validation/spec
- `2026-06-29` AIP-187: parameterize unix_local stack; keep docker-compose.yml pristine
  > Restores docker-compose.yml to its original daytona-default form (mergeable,
  > no machine-specific paths) and moves the local unix_local stack to a dedicated
  > docker-compose.unix-local.yml:
  > - self-contained (postgres + runtime; no daytona/temporal/observability infra)
  > - plugin path via ${RUNTIME_PLUGIN_HOST_PATH:-./plugins} (no hardcoded path)
  > - unix_local + privileged + scheduler/OTEL-off + .runtime-* mounts
  >
  > Usage:
  >   RUNTIME_PLUGIN_HOST_PATH=/abs/path/to/plugins \
  >     docker compose -f docker-compose.unix-local.yml up -d --build
- `2026-06-29` AIP-187: update docker-compose for unix_local sandbox configuration
  > - Disabled Daytona services for the local unix_local stack, with comments on re-enabling if needed.
  > - Set RUNTIME_SANDBOX_BACKEND to unix_local, overriding the .env configuration for local development.
  > - Added bind mounts for workspace and data directories to persist across container restarts.
  > - Configured the container to run in privileged mode to support bubblewrap and cgroups v2 requirements.
  > - Updated healthcheck and dependencies to reflect the new sandbox environment.
  >
  > This change streamlines local development by focusing on the unix_local backend, ensuring a more efficient setup without the overhead of Daytona services.
- `2026-06-29` AIP-187: unix_local — bind the runtime venv into the sandbox
  > The agent's `execute` resolves `python3`/deps from the sandbox PATH, but the
  > runtime's installed libraries live in the uv venv (`/opt/venv`), not the host
  > `/usr` the executor already binds. Bind the venv (`sys.prefix`) read-only and
  > prepend its `bin/` to PATH so bundled-skill scripts and agent code can import
  > pandas/openpyxl/etc. (FR-4/FR-5). Verified in Docker: `import pandas` works and
  > plugins resolve read-only at /<plugin>.
- `2026-06-29` AIP-187: unix_local sandbox backend (bubblewrap + cgroups v2)
  > Adds a third sandbox backend, RUNTIME_SANDBOX_BACKEND=unix_local, that runs the
  > agent's execute shell + Deep Agents file tools inside a bubblewrap mount
  > namespace on the runtime host (spec 012). Strategic replacement for the AIP-185
  > two-address local-shell workaround; Daytona stays the kernel-isolated tier.
  >
  > - Vendored runtime/harness/sandbox/unix_local/ from john221wick/deepagents-sandbox
  >   (MIT, pinned SHA; see NOTICE), patched with SandboxConfig.workspace_root so
  >   /workspace binds a stable per-user host dir. Name follows the OpenAI Agents SDK
  >   unix_local convention (ours uses bubblewrap; theirs adds no Linux confinement).
  > - UnixLocalSandboxProvider: per-user /workspace, read-only live plugin mounts at
  >   /<plugin>, host /usr (deps) read-only; CLAUDE_PROJECT_DIR=/workspace,
  >   CLAUDE_PLUGIN_ROOT=/. P-004: --clearenv + --cap-drop ALL, never binds vault.
  > …
- `2026-06-29` AIP-185: unify local-shell workspace, bake sandbox deps, per-turn plugin re-seed
  > Fixes three local-shell production issues (spec 011):
  > - execute shell now shares one workspace with the file tools via a two-address
  >   contract (file: /{session_id}/, shell: $CLAUDE_PROJECT_DIR/{session_id}/,
  >   plugins: $CLAUDE_PLUGIN_ROOT/<plugin>/), surfaced in _workspace_note
  > - runtime image bakes the bundled skills' deps (requirements-sandbox.txt +
  >   Node/pptxgenjs), mirroring docker/daytona/sandbox/Dockerfile
  > - LocalShellSandboxProvider.get re-seeds plugins every turn, content-hash gated,
  >   so source edits land without a restart
  >
  > Relates to PENG-318. Decision recorded as D-036.
- `2026-06-30` AIP-189: restore execute tool on unix_local (SessionCwdBackend protocol conformance)
  > SessionCwdBackend wraps the unix_local backend per turn but is a plain
  > delegating class, so the nominal isinstance(_, SandboxBackendProtocol)
  > check Deep Agents uses to gate the `execute` tool failed — the tool was
  > silently dropped on unix_local (and only there, since Daytona/local-shell
  > are not wrapped).
  >
  > Register SessionCwdBackend as a virtual subclass of SandboxBackendProtocol
  > so supports_execution() holds. No behavior change; adds a regression test.
- `2026-06-30` AIP-189: viewable sandbox file links + LibreChat artifacts + sharing (D-038/D-039/D-040) (#40)
  > * AIP-189: viewable sandbox file links + agent-driven presentation (D-038, Phase 1)
  >
  > Replace D-026's auto-appended public-link block with agent-driven file
  > presentation. Add authenticated GET /v1/files/workspace/{path} serving
  > inline-or-download by content type (HTML/SVG sandboxed, all inline nosniff);
  > remove the anonymous /public/{token} route and links.py. Rewrite the workspace
  > prompt block to surface files as sandbox:/ links + HTML artifact references.
  >
  > Phase 1 runtime only; Phase 1-web (sandbox:/ proxy + artifact render) and
  > Phase 2 (S3 image hosting + restricted shares) deferred.
  >
  >
  > …
- `2026-06-30` AIP-189: fix CI (ruff-format + history-narrative) and share user-global files
  > - CI ruff-format: apply formatting to chat.py + the files-API integration test.
  > - CI governance hook: reword the session_cwd docstring to drop the banned
  >   "previously" history-narrative phrase (no behavior change).
  > - New bugbot: _resolve_file now falls back to the user-global scope
  >   (session_id=NULL, D-027) like GET /v1/files/workspace, so a user-global file
  >   is shareable by path under a session.
  >
  > pre-commit run --all-files: all hooks pass; full suite green (580).
- `2026-06-30` AIP-189: bugbot #2/#6 — share current bytes; public route sniffs image bytes
  > - #6 _resolve_file prefers the live workspace (collect_one, content-addressed)
  >   over a possibly-stale registry row, so sharing a path the agent overwrote this
  >   turn yields the new content; falls back to the registry when no live backend.
  > - #2 the public image route sniffs the actual bytes (PNG/JPEG/GIF/WEBP/BMP/SVG)
  >   and serves only genuine images, with the sniffed content-type — a file_id whose
  >   bytes were overwritten with non-image content after the link was minted now 404s
  >   instead of leaking non-image data. _content_response gains a content_type override.
  >
  > Tests added; full unit+integration suite green (579).
- `2026-06-30` AIP-189: address bugbot findings — sharing gate, headless cwd, user-global links
  > - #4 restricted sharing no longer gated on the public base URL: share_tools_for
  >   registers create_share_link when public OR restricted (files_share_link_base_url
  >   + share store) is configured. New restricted_sharing_configured().
  > - #7 headless/scheduled runs now wrap the backend with the session cwd helper too
  >   (D-039), so unix_local shell cwd matches the session workspace. Extracted
  >   session_cwd_backend() shared by chat + headless.
  > - #8 workspace route falls back to the user-global scope (session_id=NULL, D-027)
  >   when the session-scoped lookup misses, so user-global files' sandbox:/ links resolve.
  > - #1 documented the public-image route's capability model (uuid4 file_id = the
  >   secret; no per-file published flag — intentional, copy-free/stateless).
  >
  > Tests added/updated; full unit+integration suite green (577).
- `2026-06-30` AIP-189: restricted-share link carries the session — <base>/<session_id>/<share_id> (D-040)
  > create_restricted_share now puts the originating session id in the link path so
  > the web-app can resolve the conversation's endpoint config (path-A style); it is
  > not used for share access (share_id + allow-list govern that). Omitted when no
  > session is in scope; runtime fallback route unchanged. Docs + test updated.
- `2026-06-30` AIP-189: make produced-file artifact note explicit (src, no inline body)
  > Clarify in _workspace_note that the HTML-report artifact references the file
  > via src="sandbox:/<path>" with NO inline body (don't paste content). This keeps
  > the runtime instruction self-contained and unambiguous against a plugin's
  > inline-content artifact guidance (coordinator-playbook artifacts.md), so the
  > agent doesn't conflate the two forms. Test updated.
- `2026-06-30` AIP-189: phrase _workspace_note as plain instruction, drop user-preference framing
  > Prompt context should be instruction/guide, not appeals to what "the user
  > wants". Reword: "working directory for user files"→"working directory",
  > "write user-facing output"→"write deliverable output", and "Surface only the
  > files the user actually wants"→"Surface only the final deliverables". Behavior
  > unchanged; tests still pass.
- `2026-06-30` AIP-189: file presentation is always on — remove RUNTIME_FILES_PRESENTATION_ENABLED
  > There was no use for an off state, so drop the toggle: remove
  > files_presentation_enabled from config, make the _workspace_note presentation
  > guidance (sandbox:/ links + HTML artifacts) unconditional, and delete the
  > disabled-path tests. Docs (D-038, file-upload-download, chat-multimodal-input),
  > spec FR-8 (withdrawn note), and .env.example updated.
- `2026-06-30` AIP-189: restricted-share links open via the web-app, not the runtime API (D-040)
  > A restricted share is served by the authenticated runtime route
  > GET /v1/files/shared/{id} (bearer + email allow-list — already correct: under
  > OIDC auth.user_id is the lowercased email, FileShare.allows is case-insensitive).
  > But create_restricted_share emitted a link pointing at that API route, which a
  > browser opening an emailed link can't authenticate against (no Authorization
  > header -> 401).
  >
  > Build the browser-facing auth where it already exists — the web-app's OAuth —
  > not in the runtime. create_restricted_share now returns
  > <files_share_link_base_url>/<share_id> pointing at the web-app /share page;
  > unset falls back to the runtime API route (proxy-only, not browser-viewable).
  > Runtime stays a pure bearer API — no OAuth-client surface added.
  > …
- `2026-06-30` AIP-189: add runtime file-presentation guide with diagrams
  > New docs/guides/file-presentation.md: a runtime-centric design reference for
  > produced-file presentation (D-038) — sandbox:/ links, HTML artifacts,
  > content-type-aware serving, and the three resolution paths (in-chat workspace
  > route, public image, restricted share). Includes a component flowchart and an
  > in-chat resolution sequence diagram, and notes the D-039 shell-cwd behavior.
  > Indexed in guides/README.md.
- `2026-06-30` AIP-187: tell unix_local agent the execute shell starts in the workspace
  > Now that SessionCwdBackend (D-039) starts the execute shell in the session
  > workspace, _workspace_note adds a unix_local-only line saying the agent is
  > already in its working directory, so relative paths (e.g. report.csv) work and
  > match the file tools. Gated on is_unix_local — not added for daytona, whose
  > shell cwd differs from snapshot.workspace.
- `2026-06-30` AIP-187: unix-local compose — export OTEL traces to JSONL files
  > Switch the lean unix-local stack from otlp-console to the file exporter
  > (.runtime-traces, bind-mounted) so traces are greppable and the console stays
  > clean during local debugging. Ignore .runtime-traces/. Set
  > RUNTIME_OTEL_EXPORTER=otlp-console to revert.
- `2026-06-30` AIP-187: run unix_local agent shell in session workspace (os.getcwd ≡ /workspace/<session>)
  > The unix_local backend is shared per user — one bubblewrap mount chdir'd to
  > the per-user root /workspace, with sessions as subdirs /workspace/<session>.
  > os.getcwd()/pwd was therefore /workspace, mismatching snapshot.workspace (the
  > path the agent is told to use and where outputs are collected), so relative
  > writes landed in the per-user root and escaped output collection.
  >
  > Add SessionCwdBackend: a per-turn proxy that prefixes every execute/aexecute
  > with `cd <snapshot.workspace> && …` and delegates all file ops (absolute
  > paths) unchanged. Wired in chat.py only when RUNTIME_SANDBOX_BACKEND=unix_local,
  > reusing the workspace dir hydration already mkdir -p'd. The proxy captures the
  > session and never mutates the shared backend, so concurrent same-user turns
  > stay isolated — the reason a per-session backend was declined in D-036.
  > …
- `2026-06-30` AIP-189: create_share_link can share a file written in the same turn (D-038)
  > The share tool resolved files only via the post-turn registry, so sharing a
  > file the agent had just written returned "no such file" (the file isn't
  > collected into the registry until StreamStop). Fix: resolve via the registry
  > OR collect the single file from the live workspace on demand.
  >
  > - hydrate.py: add FileHydration.collect_one(backend, user, session, path) —
  >   reads one workspace file now, dedups by (scope, filename, sha), persists +
  >   registers it; collect() also dedups identical-content outputs so the
  >   StreamStop pass doesn't add a duplicate row/blob.
  > - sharing.py: _resolve_file falls back to collect_one; publish_public_image /
  >   create_restricted_share take hydration + backend.
  > - share_tools_for + DeepAgentBuilder + main.py thread hydration + per-turn backend.
  > …
- `2026-06-30` AIP-189: sync .env.example with D-038 file vars
  > Drop the retired D-026 vars (RUNTIME_FILES_LINK_SECRET / _TTL_SECONDS /
  > _PUBLIC_BASE_URL) and document the current ones: RUNTIME_FILES_PRESENTATION_ENABLED
  > (Phase 1) and RUNTIME_FILES_SHARE_PUBLIC_BASE_URL / _LINK_BASE_URL (Phase 2).
- `2026-06-30` AIP-189: drop S3 backend — public images served by file_id from the data dir
  > Per review, remove the S3 publishing path and boto3 entirely. Public image
  > sharing now has a single backend: the produced image is served straight from
  > RUNTIME_FILES_DATA_DIR by file_id via the unauthenticated, image-only route
  > GET /v1/files/public/{file_id} (no copy, no object store). Enabled when
  > RUNTIME_FILES_SHARE_PUBLIC_BASE_URL is set.
  >
  > - config: remove files_share_s3_bucket/prefix/region + files_share_ttl_days.
  > - sharing.publish_public_image: no store/boto3; returns the serve-by-file-id URL.
  > - share_tools_for / DeepAgentBuilder: drop the now-unused file_store dependency.
  > - files.py: public route gated on the public base URL only.
  > - uv remove boto3 (no longer used by the runtime).
- `2026-06-30` AIP-189: restricted allow-list shares + local public-image backend (D-038 Phase 2b)
  > Restricted shares (FR-10b): create_share_link(visibility="restricted") records a
  > file_shares row (owner, file, recipient email allow-list, expiry) and returns a
  > link resolved through the gateway. New authenticated route
  > GET /v1/files/shared/{share_id} serves the OWNER's file to a requester whose
  > identity (auth.user_id, an email per D-034) is on the allow-list — the only
  > route that serves another user's file, and only the one file the share pins;
  > uniform 404 on any miss. Requester identity is gateway-forwarded (T22).
  >
  > Local public-image backend (dev, no S3): when no bucket is configured,
  > publish_public_image returns a serve-by-file-id URL and GET /v1/files/public/{file_id}
  > serves the IMAGE straight from RUNTIME_FILES_DATA_DIR — unauthenticated,
  > image-only, and active only in local mode (off when an S3 bucket is set).
  > …
- `2026-06-30` AIP-189: public image sharing via create_share_link tool (D-038 Phase 2a)
  > Add an agent tool that publishes a produced image to a public S3 prefix and
  > returns its public URL, so an emailed HTML report can reference it and external
  > recipients can load it. Off by default: registered only when the S3 bucket is
  > configured (RUNTIME_FILES_SHARE_S3_BUCKET); image content types only — raw data
  > (CSV/PDF) cannot be made public. The private in-chat workspace route never
  > touches S3.
  >
  > - runtime/harness/files/sharing.py: publish_public_image (image-only, unguessable
  >   key, boto3 put_object off the event loop).
  > - runtime/harness/files/share_tools.py: create_share_link StructuredTool
  >   (visibility=public wired; restricted is Phase 2b).
  > - Wired into the coordinator tool set via DeepAgentBuilder (file_store/registry).
  > …

## `100x-web-application` — Web app API (23)

- `2026-06-29` feat: AIP-189 resolve runtime sandbox file links + render HTML artifacts
  > Phase 1-web companion to the agent-runtime D-038 change. The runtime emits
  > produced files as `sandbox:/<path>` markdown links and HTML reports as
  > `:::artifact{type="text/html" src="sandbox:/<path>"}` references with no inline
  > body. This resolves them under the signed-in user's session:
  >
  > - Backend: GET /api/files/runtime/:conversationId/* proxies to the conversation's
  >   custom-endpoint `sandbox` base (/v1/files/workspace/{path}) with the user's
  >   bearer + x-session-id (= conversation id). Ownership is the conversation
  >   lookup; relays content-type/disposition/CSP so the runtime controls rendering.
  > - Client: rewrite `sandbox:/` markdown links to the proxy; render `src`-based
  >   HTML artifacts in a sandboxed iframe instead of Sandpack.
- `2026-06-30` fix: AIP-189 collapse CSV cell newlines to a space (MarkdownLite has no raw HTML)
  > The previous fix turned quoted-field newlines into <br>, but MarkdownLite renders
  > via react-markdown without raw-HTML support, so the tag showed literally. Collapse
  > embedded newlines to a space instead — the cell stays on one table row and reads
  > cleanly. (Bugbot.)
- `2026-06-30` fix: AIP-189 share proxy relays 401 + useSharedFile guards blob creation
  > Bugbot follow-ups on f2664f12:
  > - shared.js: relay upstream 401 as 401 (was collapsed into 404) so the client's
  >   token-refresh can fire on a recoverable auth failure, like the sandbox proxy.
  > - useSharedFile: skip createObjectURL when the effect already cleaned up —
  >   otherwise the blob URL is created after cleanup captured `created` and never
  >   revoked (leak).
- `2026-06-30` fix: AIP-189 share proxy relays 401 + useSharedFile guards blob creation
  > Bugbot follow-ups on f2664f12:
  > - shared.js: relay upstream 401 as 401 (was collapsed into 404) so the client's
  >   token-refresh can fire on a recoverable auth failure, like the sandbox proxy.
  > - useSharedFile: skip createObjectURL when the effect already cleaned up —
  >   otherwise the blob URL is created after cleanup captured `created` and never
  >   revoked (leak).
- `2026-06-30` fix: AIP-189 useSharedFile ignores stale state across share navigation
  > The route reuses SharedFileView across param changes, so the first paint after
  > opening another share still held the previous file's state until the effect ran.
  > Tag each result with the proxyUrl it was produced for and report loading when the
  > current url doesn't match, so a stale file/binary-prompt never flashes. (Bugbot.)
- `2026-06-30` fix: AIP-189 useSharedFile ignores stale state across share navigation
  > The route reuses SharedFileView across param changes, so the first paint after
  > opening another share still held the previous file's state until the effect ran.
  > Tag each result with the proxyUrl it was produced for and report loading when the
  > current url doesn't match, so a stale file/binary-prompt never flashes. (Bugbot.)
- `2026-06-30` fix: AIP-189 resolve artifact loading-state edge + config-lookup error handling
  > Bugbot follow-ups on 3c646665:
  > - ArtifactTabs: an unresolvable sandbox `src` (no conversationId / bad path)
  >   no longer shows an endless spinner — it's treated as failed and shows the
  >   error state, since no fetch ever runs.
  > - sandbox.js / shared.js: `getCustomEndpointConfig` is now inside try/catch, so
  >   a config error returns a controlled 502 instead of an unhandled 500.
- `2026-06-30` fix: AIP-189 resolve artifact loading-state edge + config-lookup error handling
  > Bugbot follow-ups on 3c646665:
  > - ArtifactTabs: an unresolvable sandbox `src` (no conversationId / bad path)
  >   no longer shows an endless spinner — it's treated as failed and shows the
  >   error state, since no fetch ever runs.
  > - sandbox.js / shared.js: `getCustomEndpointConfig` is now inside try/catch, so
  >   a config error returns a controlled 502 instead of an unhandled 500.
- `2026-06-30` fix: AIP-189 harden sandbox proxy path + CSV parsing + artifact preview states
  > Bugbot follow-ups:
  > - sandbox.js: re-encode each path segment before the upstream URL (Express
  >   decodes the splat once, so spaces/#/% in filenames were forwarded unescaped);
  >   traversal guard now decode-until-stable so multi-encoded `..` can't slip past.
  > - sandboxArtifact.ts: parse CSV in one pass so newlines inside quoted fields no
  >   longer split rows; multi-line cells render as <br> in the GFM table.
  > - ArtifactTabs.tsx: surface loading/error for sandbox fetches (was a blank panel
  >   on failure / in flight).
- `2026-06-30` fix: AIP-189 harden sandbox proxy path + CSV parsing + artifact preview states
  > Bugbot follow-ups:
  > - sandbox.js: re-encode each path segment before the upstream URL (Express
  >   decodes the splat once, so spaces/#/% in filenames were forwarded unescaped);
  >   traversal guard now decode-until-stable so multi-encoded `..` can't slip past.
  > - sandboxArtifact.ts: parse CSV in one pass so newlines inside quoted fields no
  >   longer split rows; multi-line cells render as <br> in the GFM table.
  > - ArtifactTabs.tsx: surface loading/error for sandbox fetches (was a blank panel
  >   on failure / in flight).
- `2026-06-30` fix: AIP-189 key artifacts by stable index so same-titled sandbox refs don't collide
  > Two artifacts in one turn sharing identifier/type/title (e.g. runtime artifacts
  > keyed only by a sandbox: src) collided on one artifactKey, so the side panel
  > could show the wrong/stale file. Append the stable per-mount `artifactIndex`
  > to the key (and the empty-artifact guard). Index is fixed at mount, so unlike
  > keying on `src` it doesn't orphan entries as src streams in. (Bugbot follow-up.)
- `2026-06-30` fix: AIP-189 key artifacts by stable index so same-titled sandbox refs don't collide
  > Two artifacts in one turn sharing identifier/type/title (e.g. runtime artifacts
  > keyed only by a sandbox: src) collided on one artifactKey, so the side panel
  > could show the wrong/stale file. Append the stable per-mount `artifactIndex`
  > to the key (and the empty-artifact guard). Index is fixed at mount, so unlike
  > keying on `src` it doesn't orphan entries as src streams in. (Bugbot follow-up.)
- `2026-06-30` fix: AIP-189 sandbox proxy catch returns 'Upstream error' not 'No such file'
  > A transport/axios failure returned 502 with body 'No such file', contradicting
  > the intentional upstream-5xx branch ('Upstream error') and making transport
  > failures look like a missing file. (Bugbot.)
- `2026-06-30` fix: AIP-189 sandbox proxy catch returns 'Upstream error' not 'No such file'
  > A transport/axios failure returned 502 with body 'No such file', contradicting
  > the intentional upstream-5xx branch ('Upstream error') and making transport
  > failures look like a missing file. (Bugbot.)
- `2026-06-30` fix: AIP-189 include src in artifact dedup so streamed sandbox refs render
  > Runtime artifacts carry their reference in `src` with empty `content`. The
  > Recoil dedup compared only `content`, so a streaming update that fills in `src`
  > (content still "") was skipped, leaving the side panel with a stale/missing
  > `src` and an iframe that never loads. Compare `src` too. (Bugbot.)
- `2026-06-30` fix: AIP-189 include src in artifact dedup so streamed sandbox refs render
  > Runtime artifacts carry their reference in `src` with empty `content`. The
  > Recoil dedup compared only `content`, so a streaming update that fills in `src`
  > (content still "") was skipped, leaving the side panel with a stale/missing
  > `src` and an iframe that never loads. Compare `src` too. (Bugbot.)
- `2026-06-30` feat: AIP-189 web-app — resolve sandbox:/ links, render artifacts, restricted-share page
  > Path A (in-chat): resolve sandbox:/<path> through the session-authenticated proxy
  > (sandbox.js + sandboxLink.ts), fetch with the Authorization header and render from
  > a blob: URL — links download; HTML renders in a sandboxed iframe; markdown/csv
  > render inline (sandboxArtifact.ts, ArtifactTabs, MarkdownComponents/Lite,
  > useSandboxFile).
  >
  > Path C (restricted share, D-040): /files/shared/:conversationId/:shareId page
  > (SharedFileView.tsx, routes) behind the app's OAuth, with the /api/files/shared
  > proxy (shared.js) forwarding the user's OIDC bearer; the runtime enforces the
  > email allow-list. Conversation lookup (Conversation.js) resolves the endpoint.
  >
  > Locale strings + docs (specification.md §12, diagram.md) updated.
  > …
- `2026-06-30` feat: AIP-189 web-app — resolve sandbox:/ links, render artifacts, restricted-share page
  > Path A (in-chat): resolve sandbox:/<path> through the session-authenticated proxy
  > (sandbox.js + sandboxLink.ts), fetch with the Authorization header and render from
  > a blob: URL — links download; HTML renders in a sandboxed iframe; markdown/csv
  > render inline (sandboxArtifact.ts, ArtifactTabs, MarkdownComponents/Lite,
  > useSandboxFile).
  >
  > Path C (restricted share, D-040): /files/shared/:conversationId/:shareId page
  > (SharedFileView.tsx, routes) behind the app's OAuth, with the /api/files/shared
  > proxy (shared.js) forwarding the user's OIDC bearer; the runtime enforces the
  > email allow-list. Conversation lookup (Conversation.js) resolves the endpoint.
  >
  > Locale strings + docs (specification.md §12, diagram.md) updated.
  > …
- `2026-06-30` AIP-189: doc — restricted-share link/route carry the session segment (D-040)
  > Runtime now emits <base>/<session_id>/<share_id>; update §12 to a
  > /files/shared/:conversationId/:shareId page + matching proxy that resolves the
  > endpoint from the conversation (path-A style). Session is for endpoint routing,
  > not share access (share_id + allow-list govern that).
- `2026-06-30` docs: AIP-189 restricted-share link/route carry the session segment (D-040)
  > Runtime now emits <base>/<session_id>/<share_id>; update §12 to a
  > /files/shared/:conversationId/:shareId page + matching proxy that resolves the
  > endpoint from the conversation (path-A style). Session is for endpoint routing,
  > not share access (share_id + allow-list govern that).
- `2026-06-30` AIP-189: doc — sandbox file presentation + restricted-share web-app plan (D-040)
  > Add docs/features/sandbox-file-presentation: feature spec (in-chat sandbox:/
  > proxy + artifact rendering, resolved KI-1/KI-2), comprehensive Mermaid
  > diagrams, and §12 — the implementation plan for the restricted-share web-app
  > surface (/share/:id SPA page reusing the app OAuth + /api/files/shared/:id
  > proxy). Pairs with runtime D-040 (link now points at the web-app, not the
  > runtime API).
- `2026-06-30` docs: AIP-189 sandbox file presentation + restricted-share web-app plan (D-040)
  > Add docs/features/sandbox-file-presentation: feature spec (in-chat sandbox:/
  > proxy + artifact rendering, resolved KI-1/KI-2), comprehensive Mermaid
  > diagrams, and §12 — the implementation plan for the restricted-share web-app
  > surface (/share/:id SPA page reusing the app OAuth + /api/files/shared/:id
  > proxy). Pairs with runtime D-040 (link now points at the web-app, not the
  > runtime API).
- `2026-06-30` refactor: AIP-189 rename file proxy route /api/files/runtime → /api/files/sandbox
  > Align the proxy path with the `sandbox:/` reference scheme it resolves.
  > Renames the route, its module (runtime.js → sandbox.js), and the client
  > resolver target.

## `100x-managed-plugins` — Managed customer plugins (3)

- `2026-06-29` rocky-mountain-analytics: add shared-memory maintenance docs (#14)
  > Add docs/ for the rocky-mountain-analytics plugin covering how shared org
  > memory is maintained:
  >
  > - shared-org-memory-design.md — applies the System Memory research to the
  >   plugin: skills-based shared memory, the lifecycle, and the maintenance
  >   feedback channels ranked by effectiveness.
  > - scheduled-re-verification.md — a Claude Desktop scheduled task that
  >   implements the Scheduled re-verification channel: re-checks high-relevance
  >   facts AND diffs the VireoX schema against the reference files, then proposes
  >   PR edits (read-only against shared memory).
  > - reverification-reports/2026-06-26-reverification.md — first run output.
- `2026-06-30` docs: add plugin orchestrator design (AIP-190)
  > Design docs for the plugin orchestrator — how an edited skill reaches
  > users (sorted by team) and stays correct, with no custom backend.
  >
  > - plugin-orchestrator-distribution.md: the main design (plain English) —
  >   decision (built-in plugin marketplaces + Cowork sync, no server),
  >   end-to-end workflow, concurrent-edit handling, maintenance, the two
  >   open team concerns (how an update spreads; conflict resolution).
  > - plugin-orchestrator-pm-brief.md: product/PM overview.
  > - assets/plugin-orchestrator-loop.{html,png}: the one-picture loop diagram.
  > - shared-org-memory-design.md, agent-feedback-channel-design.md: the
  >   keep-it-correct half (PENG-317), referenced by the design.
  >
  > …
- `2026-07-01` docs(AIP-190): fork-and-reconcile model + regenerated loop diagram
  > - distribution.md: merge the mechanism into the overview; copy-from-base with a
  >   pre-commit fan-out hook (skips forks); skill versioning + fork convention;
  >   drop the removed AIP-19x sub-issues (folded into the epic)
  > - agent-feedback-channel: rename report_memory_drift -> report_feedback; file into
  >   a Linear ticket (Central Feedback and Bugs) with dedupe + provenance
  > - assets: fork-and-reconcile loop card + PNG (edit-a-skill / give-feedback /
  >   scheduled-reconcile return lanes)

## `vireox-runtime-plugin` — Vireox runtime plugin (2)

- `2026-06-30` AIP-189: report-builder emails use create_share_link for images + data (D-038) (#9)
  > Emailed reports must reference assets by URLs that open outside the chat:
  > - images (image blocks/logos) → create_share_link(visibility="public") public URL
  >   in src (not base64 — keeps the body under Gmail's ~102 KB clip limit);
  > - data downloads (csv_url / "download the data") → create_share_link
  >   (visibility="restricted", recipients=[…]) so raw data stays allow-list-gated.
  >
  > Also drop the stale D-026 "runtime auto-surfaces / appends download links" notes:
  > the agent now surfaces the deliverable itself as a sandbox:/ link + HTML artifact.
- `2026-06-30` AIP-189: report-builder emails use create_share_link for images + data (D-038)
  > Emailed reports must reference assets by URLs that open outside the chat:
  > - images (image blocks/logos) → create_share_link(visibility="public") public URL
  >   in src (not base64 — keeps the body under Gmail's ~102 KB clip limit);
  > - data downloads (csv_url / "download the data") → create_share_link
  >   (visibility="restricted", recipients=[…]) so raw data stays allow-list-gated.
  >
  > Also drop the stale D-026 "runtime auto-surfaces / appends download links" notes:
  > the agent now surfaces the deliverable itself as a sandbox:/ link + HTML artifact.
