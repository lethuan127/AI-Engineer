# Week of 11 May 2026

> **Range:** Mon 11 May – Sun 17 May 2026  
> **Activity:** 25 commits across 2 repos · 12 Linear tickets

| Repository | Commits | Domain |
|---|---|---|
| `100x-agent-hub` | 24 | Backend agent platform |
| `100x-agent-hub-ui` | 1 | Frontend admin UI |

---

## 🎫 Linear tickets

**Completed (4)**

- ✅ **CENG-6** (completed) — Failure to send reports on yesterday report delivery status  
  _Done · Urgent priority, `Bug`_
- ✅ **CENG-8** (completed) — Rocky Mountain - Report / Workflow Not Found on Agent Hub  
  _Done · Urgent priority, `Bug`_
- ✅ **CENG-9** (completed) — Fix case-sensitive issue when searching workflow by email  
  _Done · Medium priority_
- ✅ **PENG-86** (completed) — [LibreChatSync] feat: Add timezone to tool Placeholder  
  _Done · — priority_

**Opened / in progress (8)**

- ✅ **AIP-1** (opened) — [LLM] Report Generation Reliability - Background Mode & Timeout Handling  
  _Done · Urgent priority_
- 📋 **AIP-10** (opened) — UI/UX - White Text on White Background - Charlie's Acccount  
  _Backlog · Low priority_
- ✅ **AIP-13** (opened) — [Infra] Isolate sampling runs on multiple agent instances  
  _Done · High priority_
- ✅ **AIP-14** (opened) — Databrick timeout  
  _Done · Low priority, `Bug`_
- 📋 **AIP-15** (opened) — Research: solution options for dynamic fan-out in workflows  
  _Backlog · Medium priority_
- 📋 **AIP-2** (opened) — Support dynamic fan-out in workflows where downstream step count depends on upstream output  
  _Backlog · Medium priority_
- ✅ **CENG-5** (opened) — HTML Failure - Charlie's Report - May 13th  
  _Done · Urgent priority, `Bug`_
- 🚫 **PENG-103** (opened) — [Reports] Switch sampling agent model to Gemini  
  _Canceled · — priority_

---

## `100x-agent-hub` — Backend agent platform (24)

- `2026-05-11` feat(auth): implement custom authentication for LiteLLM AI Gateway
  > Added a new `custom_auth.py` module to handle public MCP route authentication, allowing clients to connect without the `x-litellm-api-key`. This implementation includes JWT validation against a single Cognito User Pool, addressing the previous 500 error on unauthenticated probes by returning a compliant 401 response. Updated environment variables and configuration to support this feature, enhancing security and usability for public chat clients.
- `2026-05-11` feat(observability): introduce new tracing decorators for enhanced observability
  > Added new decorators and attributes to improve tracing capabilities within the observability framework. This enhancement aims to provide more granular insights into the system's performance and behavior, facilitating better monitoring and debugging.
- `2026-05-12` feat(auth): implement dynamic public-MCP discovery for LiteLLM AI Gateway
  > Refactored the authentication mechanism in `custom_auth.py` to support dynamic discovery of public MCP routes based on each server's `auth_type` and `available_on_public_internet` fields. This change eliminates the need for the `LITELLM_PUBLIC_MCP_PREFIXES` environment variable, enhancing usability and reducing configuration overhead. Updated documentation to reflect these changes and ensure clarity on the new public-MCP discovery process.
- `2026-05-13` feat(mcp-gateway): add architecture and design documentation for new MCP Gateway
  > Introduced comprehensive documentation for the new MCP Gateway, detailing its architecture and design principles. The architecture document outlines the gateway's responsibilities, deployment topology, and internal components, while the design document explains the rationale behind the new implementation, including the transition from the LiteLLM-based gateway. Both documents serve as foundational resources for engineers involved in the development and operation of the gateway, awaiting approval before code implementation begins.
- `2026-05-14` feat(notifications): add mage_run_id and block_run_id to transform_data function
  > Enhanced the transform_data function to include mage_run_id and block_run_id as parameters, improving the tracking of notification processes. This change allows for better observability and debugging during the notification sending workflow.
- `2026-05-14` feat(report): V3 report path on OpenAI background mode (AIP-1)
  > V2 report generation periodically fails with a 15-minute OpenAI server-side
  > timeout on the synchronous /v1/responses call. This adds a V3 fallback path
  > that runs on OpenAI's API-side background mode (responses.create with
  > background=True + poll responses.retrieve), eliminating the long-poll
  > connection that the upstream cap can sever.
  >
  > Mechanics:
  > - Upgrade Agno 2.5.15 -> 2.5.16 across vireox-agents, onehundredx-agent-skills,
  >   onehundredx-auto-evals, onehundredx-memory to pick up native OpenAI
  >   background-mode support on OpenAIResponses (PR #7444).
  > - New REPORT_V3_MODE enum (off/fallback/primary) plus REPORT_V3_POLL_INTERVAL_SECONDS
  >   (=2.0) and REPORT_V3_DEADLINE_SECONDS (=1500.0) which …
- `2026-05-14` feat(report): apply V3 background-mode patch to V2 when model is OpenAIResponses
  > Mirror ``_build_v3_agent_runtime``'s model-type-conditional patch in
  > ``_build_v2_agent_runtime``: if the resolved V2 Mongo doc returns a
  > model that happens to be an ``OpenAIResponses`` instance (i.e. the V2
  > agent was migrated to ``use_responses_api=true``), flip
  > ``model.background = True`` plus ``background_poll_interval`` and
  > ``background_max_wait`` from the same config knobs as V3.
  >
  > The default V2 path (``OpenAIChat``) is left untouched — the patch only
  > applies when the model already supports background mode.
  >
  > Why: makes V2 timeout-mitigation a Mongo-doc change instead of a code
  > change. Today V2's 15-minute upstream timeout is the original AIP-1
  > failure mode; this hands ops the same lever …
- `2026-05-14` feat(report): report-generation reliability — background mode + per-draft isolation (AIP-1)
  > * feat(report): V3 report path on OpenAI background mode (AIP-1)
  >
  > V2 report generation periodically fails with a 15-minute OpenAI server-side
  > timeout on the synchronous /v1/responses call. This adds a V3 fallback path
  > that runs on OpenAI's API-side background mode (responses.create with
  > background=True + poll responses.retrieve), eliminating the long-poll
  > connection that the upstream cap can sever.
  >
  > Mechanics:
  > - Upgrade Agno 2.5.15 -> 2.5.16 across vireox-agents, onehundredx-agent-skills,
  >   onehundredx-auto-evals, onehundredx-memory to pick up native OpenAI
  >   background-mode support on OpenAIResponses (PR #7444).
  > - New REPORT_V3_MODE enum (off/fallback/primary) plus …
- `2026-05-14` feat(report): restore V4 report structure in V5 Notification Agent
  > This commit introduces a new document outlining the restoration of the V4 report structure for the V5 Notification Agent. Key changes include reinstating the report date under the H1, adding a date suffix in the footer, adjusting section heading levels, and reintroducing the per-notification "Insights" prose-bullets section. Additionally, a code change in `_prepare_session_state` ensures the current date is accurately reflected in the report.
  >
  > New instructions for generating both HTML and Markdown reports have been added to ensure compliance with the restored structure. The previous seed script for the V3 notification agent has been removed as part of this update.
- `2026-05-15` feat(report): add Streamlit viewer and offline accuracy evaluation for V3 reports
  > This commit introduces a new Streamlit application for visualizing V3 report accuracy evaluation results. The viewer allows users to interactively explore report summaries and compare expected vs. generated HTML side-by-side.
  >
  > Additionally, an offline accuracy evaluation script has been implemented, which replays reports against the V3 prompt and model, grading the results using an LLM judge. This enhances the ability to assess changes in report generation without impacting production code paths.
  >
  > Key changes include:
  > - New `view_eval_v3.py` script for the Streamlit viewer.
  > - New `eval_v3_accuracy.py` script for offline evaluation.
  > - Helper functions in `eval_v3_helpers.py` to support the …
- `2026-05-11` fix(agent_hub): improve logging for Databricks credential handling
  > Updated logging messages to ensure sensitive information is not disclosed when fetching Databricks credentials. The changes include maintaining the original log structure while adding a nosemgrep comment to prevent credential disclosure warnings.
- `2026-05-11` fix(chat): skip tool_call entries missing tool data
  > * fix(chat): skip tool_call entries missing tool data
  >
  > When the user stops a tool mid-run, the message content can carry a
  > tool_call entry without the actual tool_call payload. Guard against this
  > by logging a warning and skipping the entry instead of dereferencing
  > None during unified conversation construction.
  >
  > * fix(agent_hub): improve logging for Databricks credential handling
  >
  > Updated logging messages to ensure sensitive information is not disclosed when fetching Databricks credentials. The changes include maintaining the original log structure while adding a nosemgrep comment to prevent credential disclosure warnings.
  >
  > ---------
- `2026-05-13` fix(api): make report and workflow email filters case-insensitive
  > Mongo queries on `email` (list_reports) and `created_by`
  > (list_report_workflows) were exact case-sensitive, hiding rows whose
  > stored value differed only in capitalization (CENG-8 root cause).
  > Switch to anchored case-insensitive regex matching the existing
  > `recipient` filter in the same file.
  >
  > Closes CENG-9
- `2026-05-14` fix(report): drop dead style param + resolve V3 prompt <br> contradiction
  > email_template._inline_style_for accepted a base_style argument it never
  > used — the regex body only references `tag`. The three call sites passed
  > _TABLE_STYLE / _TH_STYLE / _TD_STYLE as no-op arguments while the real
  > merge happened inside the `_merge_styles` lambdas. Two locations for one
  > effective constant is a drift trap: refactor the helper to take only
  > `tag`, leave the style constants at the .sub() call site as the single
  > source of truth, and update the docstring to call this out.
  >
  > The V3 system prompt simultaneously forbade all HTML ("no HTML tags",
  > "under no circumstances Emit HTML tags or inline styles") and instructed
  > the model to use <br> inside table cells for multi-line content. …
- `2026-05-14` fix(report): drop redundant semicolon when merging existing inline styles
  > The base style constants (_TABLE_STYLE, _TH_STYLE, _TD_STYLE, _HR_STYLE,
  > _ANCHOR_STYLE) all terminate with a trailing semicolon, so they double as
  > the separator when an existing style needs to be appended. The explicit
  > extra ';' in the merge produced ';;' on every '<th>'/'<td>' that
  > Python-Markdown emitted with 'style="text-align: left;"' from a ':---'
  > pipe-table column — i.e. essentially every aligned cell in V3 output.
- `2026-05-14` fix(report): escape & in email <title>; -O-safe session-id invariant
  > email_template.render_markdown_email built a "safe" subject with
  > ``.replace("<", "&lt;").replace(">", "&gt;")`` but did not escape ``&``.
  > A subject like ``"Sales & Marketing Report"`` landed in the rendered
  > ``<title>`` verbatim, which strict HTML 5 parsers and some email clients
  > treat as a broken entity reference (``& M`` -> dropped/garbled title).
  > Switch to stdlib ``html.escape(..., quote=False)`` — the canonical
  > primitive for element-text escaping, covers ``&``/``<``/``>`` in one
  > pass and won't drift. Added test_render_subject_ampersand_escaped as a
  > regression to lock the behaviour in.
  >
  > user_report_generator.send_notification guarded a control-flow invariant
  > with ``assert …
- `2026-05-14` fix(report): guard V2 arun result against empty messages and content
  > The V2 OpenAIResponses branch accessed report_res.messages[-1].content
  > unconditionally, which raises IndexError when the run completes with no
  > messages. Mirror V3's safeguards: fall back to res.content when messages
  > is empty, and raise RuntimeError on empty/whitespace content so an empty
  > draft never silently propagates into the sampling/email path.
- `2026-05-14` fix(report): normalize V2 and V3 drafts to a canonical layout for sampling
  > V2 reports passed to select_best_report retained the
  > 'EMAIL_SUBJECT:' + 'REPORT_HTML_CONTENT:' wrapper while V3 reports
  > retained 'EMAIL_SUBJECT:' + 'REPORT_MARKDOWN_CONTENT:'. The differing
  > protocol marker leaked into the sampling agent's prompt and could
  > nudge the comparison in fallback mode where the two formats compete
  > for the same slot.
  >
  > Replace _convert_report_html_to_markdown with
  > _normalize_report_for_sampling(content, source) which strips the
  > upstream protocol wrapper for both branches and returns a single
  > 'SUBJECT: <s>\n\n<markdown>' shape. The reports list returned
  > downstream still carries the original wrapper, so the email-send
  > parsers (_extract_v2_email_parts / …
- `2026-05-14` fix(report): route V2 OpenAIResponses agents through non-streaming arun
  > The V2 background-mode patch in 5052096 was dead code: it sets
  > background=True on the model, but the V2 callsite invokes the agent via
  > arun_stream_to_run_output which forces stream=True. Agno's
  > OpenAIResponses.ainvoke_stream silently strips background on streaming
  > requests (responses.py:1062), so V2 stayed exposed to the upstream
  > 15-min timeout even with use_responses_api=true flipped on the Mongo
  > doc.
  >
  > Branch _generate_single_report_v2 by model class:
  >
  > - OpenAIResponses -> plain agent.arun(), hitting Agno's non-streaming
  >   ainvoke path which actually drives responses.create(background=True,
  >   store=True) + polling.
  > - OpenAIChat -> arun_stream_to_run_output as before; streaming keeps
  >   the …
- `2026-05-14` refactor(report): per-draft agent isolation for V2 + V3 sampling (AIP-13)
  > V2's 5-draft `asyncio.gather` fan-out shared one `Agent` instance
  > serialized by `_get_report_stream_lock`, defeating parallelism: the
  > streaming helper mutates `agent.parse_response` inline, so concurrent
  > calls would corrupt each other without the lock. V3 shared one agent
  > without any lock at all, surviving by accident of agno 2.5.16's internal
  > layout (`cache_session=False` default, no agent-level metadata, etc.).
  >
  > Both paths now build a fresh `Agent` per draft:
  >
  > - `_resolve_v{2,3}_agent_record()` — cached Mongo probe; 5x fan-out
  >   reads Mongo exactly once per send_notification.
  > - `_build_v{2,3}_agent_runtime(session_id)` — factory call returning a
  >   new `Agent` every time. V3 additionally …
- `2026-05-14` chore(agents): fix semgrep nosemgrep directive placement
  > The two `# nosemgrep: python-logger-credential-disclosure` comments in
  > `AgentHubFactory._enrich_authorized_locations` sat on the *continuation*
  > line of the `logger.info(...)` / `logger.exception(...)` call. Semgrep
  > matches the whole call expression starting at the `logger.…(` line, so
  > the comment was placed where Semgrep doesn't look and the rule kept
  > firing — blocking the security-scan job on PRs.
  >
  > Move the directive to the line immediately above the call (Semgrep's
  > "line above the match start" form) and use the fully-qualified rule ID
  > `python.lang.security.audit.logging.logger-credential-leak.python-logger-credential-disclosure`
  > so the suppression survives a registry alias …
- `2026-05-14` chore(deps): bump urllib3 constraint 2.6.3 -> 2.7.0
  > Patches two HIGH-severity CVEs flagged by Trivy on `uv.lock` which were
  > blocking the security-scan job:
  >
  > - CVE-2026-44431 — sensitive headers forwarded across origins in
  >   proxied low-level redirects.
  > - CVE-2026-44432 — decompression-bomb safeguards bypassed in parts of
  >   the streaming API.
  >
  > Workspace constraint bumped in the root `pyproject.toml`; lockfile
  > re-resolved via `uv lock --upgrade-package urllib3`. urllib3 is a
  > transitive of `requests` / `httpx` / `botocore` etc. — no first-party
  > import changes needed. Local test smoke (onehundredx-report 63/63,
  > vireox-agents factory tests 16/16) green.
- `2026-05-14` test(report): stub network surfaces at conftest import time
  > `vireox_agents.tools.entity_clarify` and `vireox_common_libs.auth.oidc`
  > instantiate module-level singletons that make real network calls at
  > import: OpenSearch `info()` and OIDC discovery `httpx.get(...)`. With
  > the placeholder `OPENSEARCH_HOST=localhost` / `OPENID_CONFIG_URL=http://localhost:0/oidc`
  > env values pytest collection crashes on dev machines where localhost:443
  > has a TLS listener (returns malformed SSL) or refuses (httpx.ConnectError).
  >
  > Stub `opensearch_client` and intercept the OIDC discovery `httpx.get`
  > before any test module is collected. Pure host-portability fix; no
  > test-runtime behaviour changes.
- `2026-05-13` Merge pull request #391 from 100xteam-ai/thuan/ceng-9-fix-case-sensitive-issue-when-searching-workflow-by-email
  > fix(api): make report and workflow email filters case-insensitive

## `100x-agent-hub-ui` — Frontend admin UI (1)

- `2026-05-14` feat(agents): add Report Generation V3 feature to agents
  > - Introduced Report Generation V3 as a new application feature in the agents' UI.
  > - Updated the ApplicationFeature enum to include REPORT_GENERATION_V3.
  > - Enhanced the AgentsPage and ApplicationFeatureTab components to support the new feature selection and display.
  > - Modified the AgentCard component to format and display the new Report Generation V3 option.
